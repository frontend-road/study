{"id":913628,"title":"日志上传：如何高效接收海量上传的日志？","content":"<p>你好，我是徐长龙。</p><p>今天我们来探讨高并发系统中的一个核心挑战——如何高效接收海量上传的日志。</p><p>在广告监测和日志采集领域，我们需要实时汇聚来自客户端的日志数据，通过分析这些数据来优化客户服务。这就要求日志收集服务必须具备高性能的数据接收能力，同时还要兼顾成本效益，这构成了系统设计的主要矛盾。</p><h2>传统方案面临的三大瓶颈问题</h2><p>面对海量日志上传的业务需求，传统方案面临哪些瓶颈问题呢？我们先分析一下。</p><p>首先是磁盘I/O瓶颈。传统方式直接将请求写入磁盘，但受限于磁盘IOPS性能。机械硬盘每秒仅能支持100-200次操作，严重制约了系统并发性能。即便使用SSD固态硬盘，在面对数万QPS时仍然力不从心，并且这种频繁的写入磁盘操作对磁盘寿命影响极大。</p><p>相信你很容易想到使用内存聚合，降低磁盘压力的方案。的确，使用内存确实可以提升性能，但是不同的内存缓存实现差距很大。特别是在使用高级语言实现时，内存占用更加显著、在上万并发场景下，不合理的实现会消耗数十GB内存，而且性能不佳，还会大幅增加资源成本和维护难度。</p><p>另外还有并发写入磁盘问题。多进程同时写入同一文件时会引发锁竞争，这会导致数据错乱。如果错误的加锁会限制系统的写入性能，从而影响整体并发处理能力。</p><!-- [[[read_end]]] --><p>正因如此，实现一个低成本好维护高性能的日志接收系统其实并不容易。为解决这些问题，我将提供一种简单高效的解决方案供你参考。</p><h2>方案设计与要点梳理</h2><p>为应对上述挑战，我们需要构建一个高性能、低成本的日志接收系统，以高效处理海量请求。接下来我们会完成方案设计，实现每天处理超10亿用户行为事件（如点击与曝光）、峰值 QPS 超过 5 万、支持数据实时落盘，并保障 99.9% 请求成功率。</p><p>该方案的核心技术选型为 OpenResty 结合 Lua，我们来看看这两个技术的主要优势。</p><p>OpenResty 基于 Nginx，继承了其高效的事件驱动架构，天然支持高并发请求处理。它内置 了Lua 脚本支持，使开发者能够灵活编写定制化逻辑，同时提供异步 I/O 机制，特别适用于密集网络与存储操作。此外，OpenResty 还集成丰富的扩展模块，涵盖缓存、负载均衡等常用功能，极大提升了开发效率和系统可扩展性。</p><p>Lua 语言则以简洁的语法和高效的执行性能著称，可大幅降低开发与维护成本。其轻量级特性和共享内存管理机制，进一步优化了资源使用和并发性能。OpenResty 配合 Lua 还具备良好的生态支持，包括活跃的社区、完善的文档，以及与其他数据库和消息队列系统的便捷集成能力。</p><p>通过 OpenResty 与 Lua 的有机结合，我们能够有效解决高并发日志接收中的性能、成本与稳定性问题，构建一个既高效又可靠的日志收集系统。</p><p><img src=\"./images/913628_1.jpg\" alt=\"\"><br>\n在我们的系统设计中，有几个关键设计要点（具体内容你可以查看后面的表格），来帮助我们构建一个高效、可靠的日志接收系统，满足高并发处理的需求。</p><p><img src=\"./images/913628_2.jpg\" alt=\"\"></p><h2>实战代码讲解</h2><p>接下来，我们将结合代码进行详细讲解，也推荐你实践一下，以便更好地体会这个方案的优势。这里需要你对 Lua 编程有一定的了解。如果还没有学过 Lua，可以借助AI学习一下，也可以在留言区交流讨论。</p><p>现在，让我们开始边读代码边讲解这个方案，一步步完成这个高效的日志接收系统。通过实际的代码示例，我们将深入探讨每个设计要点及其实现方式。</p><h3>环境准备</h3><p>我们将在 Ubuntu 环境下进行实战操作。</p><p>首先，你需要安装 OpenResty 的基础环境。可以使用以下命令。</p><pre><code class=\"language-plain\">sudo apt-get install openresty\n</code></pre><p>接下来，安装 Lua 包管理器。</p><pre><code class=\"language-plain\">sudo apt-get install luarocks\n</code></pre><p>然后，安装所需的 Lua 组件。</p><pre><code class=\"language-plain\">sudo luarocks install luafilesystem\nsudo opm get ledgetech/lua-resty-http\nsudo opm get openresty/lua-resty-lock\n</code></pre><p>最后，重启 OpenResty 服务以确保所有更改生效：</p><pre><code class=\"language-plain\">sudo systemctl restart openresty\n</code></pre><p>完成以上步骤后，环境准备工作就搞定了。</p><h3>Nginx配置</h3><p>接下来对Nginx进行配置，OpenResty的主体还是通过Nginx提供服务的，Nginx的配置对应的文件在 /Usr/Local/Openresty/Nginx/Conf/Nginx.conf 或 /etc/openresty/ngxin/conf/nginx.conf中。</p><p>下面就是我们的一个配置实现，这个配置会设置一个定时任务，定期将共享队列中的数据落到磁盘。除此之外，设置了一个域名和api地址为 report.example.com/report ，这个配置的脚本会将所有请求的body保存在队列中。具体代码是后面这样。</p><pre><code class=\"language-lua\">http {\n    # 设置Lua模块路径，下文的flush_handler.lua文件就是从这里加载的\n    lua_package_path \"/path/to/lua/scripts/?.lua;;\";\n    \n    # 400MB共享内存用于队列\n    lua_shared_dict report_queue 400m;\n    # 写日志锁，写日志时是单线程append实现，性能更好\n    lua_shared_dict file_locks 1m;\n    \n    # 上传请求body的大小限制\n    client_max_body_size 10M;\n    proxy_max_temp_file_size 10m;\n\n    # 初始化worker进程\n    init_worker_by_lua_block {\n        local flush_handler = require \"flush_handler\"\n        local function schedule()\n            ngx.timer.at(5, flush_handler)  -- 每5秒执行落盘\n        end\n        schedule()\n        ngx.log(ngx.INFO, \"Flush handler initialized\")\n    }\n\n    server {\n        listen 80;\n        server_name report.example.com;\n        \n        # 关闭访问日志减少I/O\n        access_log off;\n        error_log logs/error.log crit;\n\n        location /report {\n            client_max_body_size 10M;\n            content_by_lua_file /path/to/lua/scripts/report.lua;\n        }\n    }\n}\n</code></pre><h3></h3><h3>上报请求处理脚本 (report.lua)</h3><p>下面我们讲解下接收请求的Lua脚本。当Nginx收到上传日志请求时，这个脚本会处理请求，具体来说，会将body内容解析出来，并保存在对应域名的内存队列内。这里不推荐你做太多解析操作，因为这会影响QPS性能。</p><pre><code class=\"language-lua\">ngx.header.content_type = \"application/json\"\nngx.req.read_body()\n\nlocal client_ip = ngx.var.remote_addr\nlocal host = ngx.var.host or \"default\"\n\nlocal body = ngx.req.get_body_data()\nif not body then\n&nbsp; &nbsp; return ngx.exit(400)\nend\n\nif #body &gt; 10 * 1024 * 1024 then\n&nbsp; &nbsp; return ngx.exit(413)\nend\n\nlocal shm = ngx.shared.report_queue\nlocal entry_count = 0\n\nfor entry in body:gmatch(\"[^\\n]+\") do\n&nbsp; &nbsp; entry = entry:gsub(\"^%s*(.-)%s*$\", \"%1\")\n\n&nbsp; &nbsp; if #entry &gt; 10 and entry:sub(1,1) == \"{\" and entry:sub(-1) == \"}\" then\n&nbsp; &nbsp; &nbsp; &nbsp; local enriched_entry = string.format(\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; '{\"timestamp\":%d,\"client_ip\":\"%s\",\"data\":%s}',\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; ngx.now() * 1000,\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; client_ip,\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; entry\n&nbsp; &nbsp; &nbsp; &nbsp; )\n\n&nbsp; &nbsp; &nbsp; &nbsp; local ok, err = shm:lpush(host, enriched_entry)\n&nbsp; &nbsp; &nbsp; &nbsp; if not ok then\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; ngx.log(ngx.ERR, \"队列写入失败: \", err)\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; return ngx.exit(503)\n&nbsp; &nbsp; &nbsp; &nbsp; end\n\n&nbsp; &nbsp; &nbsp; &nbsp; entry_count = entry_count + 1\n&nbsp; &nbsp; end\nend\n\nif entry_count &gt; 0 then\n&nbsp; &nbsp; ngx.say(string.format('{\"code\":0,\"msg\":\"success\",\"accepted\":%d}', entry_count))\nelse\n&nbsp; &nbsp; ngx.say('{\"code\":101,\"msg\":\"no valid data\"}')\nend\n</code></pre><h3>定时落盘脚本 (Flush_handler.lua)</h3><p>好，我们再来看看定时落盘脚本，它是前面在Nginx中定义的定时任务，每隔5秒就会执行一次，用途是将队列中的数据批量写入磁盘。</p><pre><code class=\"language-lua\">local resty_lock = require \"resty.lock\"\nlocal lfs = require \"lfs\"\n\n-- 确保目录存在（递归创建）\nlocal function ensure_directory(dir)\n    local current = \"\"\n    for part in dir:gmatch(\"[^/]+\") do\n        current = current .. \"/\" .. part\n        if not lfs.attributes(current) then\n            local ok, err = lfs.mkdir(current)\n            if not ok then\n                ngx.log(ngx.ERR, \"目录创建失败: \", current, \" 错误: \", err)\n                return false\n            end\n        end\n    end\n    return true\nend\n\n-- 批量写入文件\nlocal function batch_write(file_path, entries)\n    local file, err = io.open(file_path, \"a\")  -- 追加模式\n    if not file then\n        ngx.log(ngx.ERR, \"文件打开失败: \", file_path, \" 错误: \", err)\n        return false\n    end\n\n    -- 批量写入优化性能\n    local content = table.concat(entries, \"\\n\") .. \"\\n\"\n    local ok, err = file:write(content)\n    if not ok then\n        ngx.log(ngx.ERR, \"文件写入失败: \", file_path, \" 错误: \", err)\n        file:close()\n        return false\n    end\n    \n    file:close()\n    return true\nend\n\n-- 主处理函数\nlocal function handler(premature)\n    if premature then return end\n    \n    local shm = ngx.shared.report_queue\n    local now = ngx.now()\n    local date_str = os.date(\"%Y-%m-%d\", now)\n    local hour_str = os.date(\"%H\", now)\n    \n    -- 获取所有域名队列\n    local keys, err = shm:get_keys(0)\n    if not keys then\n        ngx.log(ngx.ERR, \"获取队列keys失败: \", err)\n        goto reschedule\n    end\n    \n    for _, domain in ipairs(keys) do\n        -- 跳过系统key\n        if domain ~= \"file_lock\"  then\n            local queue_len = shm:llen(domain)\n            if queue_len == 0 then goto continue end\n            \n            -- 获取分布式锁（按域名分区）\n            local lock = resty_lock:new(\"file_locks\", {\n                timeout = 3000,   -- 3秒超时\n                exptime = 10000   -- 10秒自动释放\n            })\n            \n            local elapsed, err = lock:lock(domain)\n            if not elapsed then\n                ngx.log(ngx.WARN, \"获取锁失败: \", domain, \" 错误: \", err)\n                goto continue\n            end\n            \n            -- 批量取出数据（最多10000条）\n            local entries = {}\n            local pop_count = math.min(queue_len, 10000)\n            \n            for i = 1, pop_count do\n                local entry = shm:rpop(domain)\n                if entry then\n                    table.insert(entries, entry)\n                else\n                    break\n                end\n            end\n            \n            -- 创建分级目录\n            local log_dir = string.format(\n                \"/data/logs/%s/%s/%s\", \n                domain, date_str, hour_str\n            )\n            \n            if not ensure_directory(log_dir) then\n                lock:unlock()\n                goto continue\n            end\n            \n            -- 生成唯一文件名\n            local file_name = string.format(\n                \"report_%s_%s_%s.log\",\n                os.date(\"%Y%m%d%H%M\"),\n                ngx.worker.pid(),\n                ngx.now() * 1000\n            )\n            local file_path = log_dir .. \"/\" .. file_name\n            \n            -- 执行写入\n            local ok = batch_write(file_path, entries)\n            if not ok then\n                -- 失败时回退数据\n                for i = #entries, 1, -1 do\n                    shm:lpush(domain, entries[i])\n                end\n            end\n            \n            lock:unlock()\n        end\n        \n        ::continue::\n    end\n    \n    ::reschedule::\n    -- 5秒后重新执行\n    ngx.timer.at(5, handler)\nend\n\nreturn handler\n</code></pre><p>这个脚本会定期扫描各个域名对应的队列，如果队列内有日志，脚本会批量地读取队列日志并append到对应文件日志内。</p><h2></h2><h2>磁盘写入底层优化</h2><p>通过我们的实现，成功降低了网络通讯带来的系统损耗，节省了系统内存和服务器数量，并通过内存合并优化了磁盘写入性能。那么，为什么需要优化磁盘写入呢？</p><p>首先，机械硬盘存在以下性能瓶颈。</p><ul>\n<li>\n<p><strong>寻道时间</strong>：约 10ms，即磁头移动到目标位置的时间。</p>\n</li>\n<li>\n<p><strong>随机 I/O 操作限制</strong>：每秒最多只能处理 100-200 次随机 I/O 操作。</p>\n</li>\n<li>\n<p><strong>顺序写入速度</strong>：可达 100MB/s 以上，但受限于随机操作的性能。</p>\n</li>\n</ul><p>此外，大量上报系统还面临文件系统的一些缺点。</p><ul>\n<li>\n<p><strong>小文件问题</strong>：产生大量 inode（文件系统元数据），增加存储开销。</p>\n</li>\n<li>\n<p><strong>元数据操作消耗</strong>：创建和删除文件时，元数据操作消耗较大。</p>\n</li>\n<li>\n<p><strong>目录文件过多</strong>：导致查找效率下降，影响整体系统性能。</p>\n</li>\n</ul><h3>对应问题的优化策略</h3><p>通过优化磁盘写入策略，我们可以有效应对这些挑战，提高系统的整体性能和可靠性。具体优化策略如下。</p><p>第一，设置日志批量写入&nbsp;。如下代码所示，通过内存合并强化磁盘日志落盘的性能。</p><pre><code class=\"language-lua\">local content = table.concat(entries, \"\\n\") .. \"\\n\" \nfile:write(content) -- 单次写入代替多次小写入\n</code></pre><p>第二，文件使用追加模式。追加模式的优势是减少磁头移动，同时避免文件覆盖操作。</p><pre><code class=\"language-lua\">local file = io.open(path, \"a\") -- 'a' 表示追加模式\n</code></pre><p>第三，目录分级。比如后面这样的结构。</p><pre><code class=\"language-lua\">/data\n&nbsp; └── logs\n&nbsp; &nbsp; &nbsp; └── example.com\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; └── 2023-06-25\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; ├── 00&nbsp; &nbsp;# 小时目录\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; │&nbsp; &nbsp;├── report_202306250000_w1.log\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; │&nbsp; &nbsp;└── report_202306250005_w2.log\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; └── 01\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; ├── report_202306250100_w1.log\n&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; └── ...\n\n</code></pre><p>目录分级的好处包括以下几点。</p><ul>\n<li>\n<p>避免单目录文件过多</p>\n</li>\n<li>\n<p>提高文件系统效率</p>\n</li>\n<li>\n<p>时间窗口分割</p>\n</li>\n<li>\n<p>按小时组织目录、平衡文件数量和大小</p>\n</li>\n</ul><h2>生产环境最佳实践</h2><p>为了确保服务的稳定性，最后我再分享四条小技巧。</p><h3>1. 内存监控</h3><p>由于使用了共享内存作为请求汇总，内存是稀缺资源。在某些情况下，可能会出现内存不足的情况。可以通过以下方式监控内存使用情况，及时发现并处理问题。</p><pre><code class=\"language-plain\">local free_bytes = shm:free_space()\nif free_bytes &lt; 10 * 1024 * 1024 then  -- 当剩余内存小于10MB时告警\n    ngx.log(ngx.ALERT, \"共享内存不足! 仅剩 \"..free_bytes..\" 字节\")\nend\n</code></pre><h3>2. 优雅降级</h3><p>要求客户端对返回值进行检测，可以在服务端内存满时，暂时将数据保存在本地并定期重试，以提高服务的可用性，避免数据丢失。例如，处理队列溢出的逻辑：</p><pre><code class=\"language-plain\">if shm:llen(domain) &gt; 50000 then\n    ngx.log(ngx.WARN, \"队列溢出: \"..domain)\n    return ngx.exit(503)  -- 返回服务不可用\nend\n</code></pre><h3>3. 文件压缩归档</h3><p>服务器磁盘空间有限，需定期对生成的日志文件进行压缩或删除，以释放空间。例如，可以设置定时任务，每小时压缩上一小时的日志：</p><pre><code class=\"language-plain\"># 每小时压缩上一小时日志\n0 * * * * find /data/logs -name \"*.log\" -mmin +60 -exec gzip -f {} \\;\n</code></pre><h3>4. 监控指标</h3><p>监控队列中的数据数量和其他关键指标，可以帮助及时发现潜在问题。例如：</p><pre><code class=\"language-plain\">-- 记录关键指标\nshm:incr(\"stats:total_entries\", #entries)  -- 总条目数\nshm:incr(\"stats:total_bytes\", #content)    -- 总字节数\n</code></pre><p>通过实施以上最佳实践，可以有效提高服务的稳定性和可用性，及时监控和处理潜在问题，确保系统高效运行。</p><h2>总结</h2><p>通过今天的课程，我们实现了一个高并发的上传系统方案，设计重点包括后面这四点。</p><ul>\n<li>\n<p><strong>高性能架构</strong>：基于 OpenResty 的高并发上传处理。</p>\n</li>\n<li>\n<p><strong>优化策略</strong>：内存缓冲 + 批量落盘的核心思想。</p>\n</li>\n<li>\n<p><strong>分布式协作</strong>：多 worker 协调处理。</p>\n</li>\n<li>\n<p><strong>磁盘优化</strong>：底层 I/O 性能提升技巧。</p>\n</li>\n</ul><h3></h3><p>需要你把握的核心思想有三条。</p><ul>\n<li>\n<p><strong>用内存换 I/O</strong>：减少磁盘操作次数。</p>\n</li>\n<li>\n<p><strong>用批量换效率</strong>：合并小操作为大操作。</p>\n</li>\n<li>\n<p><strong>用分区换并发</strong>：分散处理避免冲突。</p>\n</li>\n</ul><p>这种设计可扩展到各种高并发数据采集场景，比如日志收集系统、用户行为跟踪、IoT 设备数据处理以及实时监控系统</p><p>希望这个方案能够帮助大家在项目实践中，低成本地解决高并发上传的挑战！</p><h3>思考题</h3><ol>\n<li>\n<p><strong>内存映射文件优化</strong>：如何进一步优化使用 <code>mmap</code> 内存映射文件的性能？在绕过内核缓冲区和减少数据拷贝次数的基础上，还有哪些措施可以提高与文件系统交互的效率？</p>\n</li>\n<li>\n<p><strong>WAL（Write-Ahead Logging）机制</strong>：在实现 WAL 机制时，除了确保数据不丢失和提供故障恢复能力外，还有哪些其他因素需要考虑？如何设计 WAL 机制以提高写入性能和恢复效率？</p>\n</li>\n<li>\n<p><strong>数据压缩与传输</strong>：客户端在上传数据之前进行压缩的过程有哪些潜在的挑战？在服务端解压处理时，如何确保解压效率与系统性能之间的平衡？此外，是否有其他的方法可以进一步提升网络传输的效率？</p>\n</li>\n</ol><p>期待你在留言区和我交流讨论。如果今天的内容对你有启发别忘了分享给身边更多朋友。</p>","comments":[]}