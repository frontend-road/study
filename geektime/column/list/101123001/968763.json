{"id":968763,"title":"25｜三级故障转移链设计：如何保证 Agent 永不掉线？","content":"<p>你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。</p>\n<p>上节课我们建立了完整的上下文防护认知：五层递进式防护体系，从启动前检查到运行时截断，确保 Agent 不会因为 Token 爆炸而崩溃。但 Token 爆炸只是 Agent 面临的挑战之一。今天我们要面对另一个更棘手的问题：LLM API 本身出了问题。</p>\n<p>当你的 Agent 正在帮用户重构一个大型项目，已经执行了十几步操作，修改了文件、运行了测试、生成了报告。然后到了第十五步的 API 调用失败，如果系统不做任何处理，你看到的就是“模型不可用”，你无法控制 API 提供商的服务状态。</p>\n<p>今天我们来解决这个问题：OpenClaw 如何设计一条完整的三级故障转移链，让 Agent 在面对 API 故障时“静默恢复”，用户甚至不需要知道底层发生了模型切换。</p>\n<h2>为什么需要三级故障转移？</h2>\n<p>在深入细节之前，一个直觉性的问题：为什么要搞这么复杂？直接“失败就换模型”不行吗？</p>\n<p>这种简化方案有两个严重问题：</p>\n<p>第一是<strong>浪费</strong>。如果只是 API Key 被限流了，换一个 Key 就能解决，换模型是大材小用。就像你家停电了，第一反应应该是检查保险丝，而不是搬家。</p>\n<p>第二是<strong>风险</strong>。不同模型的行为可能有差异，GPT-4o 和 Claude 对同一个 prompt 的理解可能完全不同。频繁切换模型会导致推理质量不稳定，用户会觉得“这个 Agent 一会儿聪明一会儿傻”。</p><!-- [[[read_end]]] -->\n<p>所以三级设计的核心思想是：<strong>能不换模型就不换模型</strong>。先试最轻量的方案，不行再逐级升级，实在不行才动用最后的手段。这就像医院的急诊分诊，不是所有病人都需要进 ICU，大多数情况下门诊就能解决。</p>\n<p>首先让我们建立三级故障转移架构全局认知，故障转移链遵循<strong>代价最小优先原则</strong>，详细情况如下：</p>\n<p><img src=\"./images/968763_1.png\" alt=\"\"></p>\n<p><strong>Level 1：认证轮换（Auth Profile Rotation）</strong></p>\n<p>这是代价最低的恢复策略。当系统检测到 HTTP 401（认证失败）、402（账单问题）、429（限流）或 503（服务过载）时，首先尝试的不是换模型，而是换一个 API Key。</p>\n<p>为什么这能解决问题？因为大多数 API 提供商的限流是针对单个 Key 的。你可能有多个 Key 分布在不同的账户上，或者同一账户的不同 Profile。只要有一个 Key 还能用，Agent 就能继续工作。这个策略的代价几乎为零，模型不变，只换认证凭证，用户完全无感知。</p>\n<p><strong>Level 2：Thinking 降级</strong></p>\n<p>这是针对特定错误的参数调整策略。有些模型支持不同级别的“思考深度”参数（比如 Claude 的 extended thinking），但这个参数在某些情况下可能不被支持。</p>\n<p>当系统检测到错误消息中包含 thinking level not supported 这样的提示时，会自动解析错误中提到的支持值，然后降级到一个兼容的参数重试。这个策略的代价是推理能力可能略降，但至少能完成任务。</p>\n<p><strong>Level 3：模型 Fallback</strong></p>\n<p>这是最后的手段，当所有 Auth Profile 都用尽了，或者错误持续发生无法恢复时，系统才会切换到备选模型。这个策略的代价最高，不同模型可能有行为差异，但至少保证了 Agent 不会完全停摆。</p>\n<p>在这个架构中，还有有一条特殊规则：<strong>上下文溢出不触发模型 Fallback</strong>。因为上下文溢出意味着当前的消息太多，如果你从 Claude换到 GPT-4o情况只会更糟。上下文溢出应该走五层防护路径（压缩、截断），而不是走模型 Fallback 路径。这是两条故障恢复路径的<strong>关键隔离点</strong>，这一点对于理解整个系统的协同设计至关重要。</p>\n<p>了解了架构全景，我们深入第一个基础能力错误分类系统。</p>\n<h2>错误分类系统：10 种 FailoverReason</h2>\n<p>三级故障转移的每一级都依赖精确的错误分类来决定触发哪一级。分类不准，整个转移链就会做出错误决策。</p>\n<p>系统是如何对错误进行分类的？核心定义如下：</p>\n<pre><code class=\"language-plain\">export type FailoverReason =\n &nbsp;| \"auth\" &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// 认证失败（API Key 无效）\n &nbsp;| \"auth_permanent\" &nbsp; &nbsp;// 永久认证问题（403 Forbidden）\n &nbsp;| \"format\" &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// 格式错误（400，如 thinking level 不支持）\n &nbsp;| \"rate_limit\" &nbsp; &nbsp; &nbsp; &nbsp;// 限流（429 Too Many Requests）\n &nbsp;| \"overloaded\" &nbsp; &nbsp; &nbsp; &nbsp;// 服务过载（503 Service Unavailable）\n &nbsp;| \"billing\" &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; // 账单问题（402 Payment Required）\n &nbsp;| \"timeout\" &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; // 超时（连接超时/请求超时）\n &nbsp;| \"model_not_found\" &nbsp; // 模型不存在（404）\n &nbsp;| \"session_expired\" &nbsp; // 会话过期（410 Gone）\n &nbsp;| \"unknown\"; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// 未知原因\n</code></pre>\n<p>每种 reason 对应不同的处理策略如下所示，这就是为什么分类必须精确：</p>\n<p><img src=\"./images/968763_2.png\" alt=\"图片\"></p>\n<p>具体处理策略中，<code>auth</code> 和 <code>rate_limit</code> 都会换 Profile，这是 L1 级别的恢复；<code>format</code> 会尝试 Thinking 降级，这是 L2 级别；而 <code>model_not_found</code> 会直接切换模型，这是 L3 级别。</p>\n<p>可能这里你会有一个疑问，为什么需要专门设计一个错误类？这是因为原始的 API 错误千奇百怪，有的是 HTTP 状态码，有的是字符串消息，有的是嵌套的 error.cause 链。<code>FailoverError</code> 类的设计，把这些混乱的错误统一成一个结构化对象：</p>\n<pre><code class=\"language-plain\">export class FailoverError extends Error {\n &nbsp;readonly reason: FailoverReason; &nbsp;// 分类原因\n &nbsp;readonly provider?: string; &nbsp; &nbsp; &nbsp; // 哪个 Provider\n &nbsp;readonly model?: string; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// 哪个模型\n &nbsp;readonly profileId?: string; &nbsp; &nbsp; &nbsp;// 哪个 Auth Profile\n &nbsp;readonly status?: number; &nbsp; &nbsp; &nbsp; &nbsp; // HTTP 状态码\n &nbsp;readonly code?: string; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; // 错误码\n}\n</code></pre>\n<p>有了这个封装，后续的故障转移逻辑就可以用简单的 <code>switch(error.reason)</code> 做清晰的策略选择，而不用每次都去解析原始错误的各种格式。</p>\n<p>那么OpenClaw具体是如何实现把五花八门的原始错误准确地分类成这 10 种 FailoverReason？</p>\n<p>下面我们详细讲解OpenClaw的五层递进的检测逻辑。因为不同的 LLM API Provider 返回错误的方式完全不同：</p>\n<ul>\n<li>\n<p><strong>Anthropic</strong>：通常返回规范的 HTTP 状态码</p>\n</li>\n<li>\n<p><strong>OpenAI</strong>：有时在 error.code 中返回 RESOURCE_EXHAUSTED</p>\n</li>\n<li>\n<p><strong>Google Gemini</strong>：用符号错误码 RESOURCE_EXHAUSTED</p>\n</li>\n<li>\n<p><strong>自托管模型</strong>：可能返回网络级错误（ETIMEDOUT、ECONNRESET）</p>\n</li>\n<li>\n<p><strong>代理服务（OpenRouter）</strong>：可能把原始错误包装在 error.cause 链中</p>\n</li>\n</ul>\n<p>一个看似简单的“检测限流”操作，因为这种异构性，OpenClaw设计了一套五层的递进检测系统，详细流程如下：</p>\n<p><img src=\"./images/968763_3.png\" alt=\"\"></p>\n<p><strong>第一层：</strong>检查错误是否已经是 FailoverError。如果是，说明已经被分类过了，直接使用。</p>\n<p><strong>第二层：</strong>尝试从 HTTP 状态码推断原因。这是最可靠的方式，因为 HTTP 状态码是标准化的。402 一定是账单问题，429 一定是限流。</p>\n<p><strong>第三层：</strong>检查符号错误码。有些 Provider 不返回 HTTP 状态码，而是在错误对象中设置一个 code 字段，比如 <code>RESOURCE_EXHAUSTED</code>。</p>\n<p><strong>第四层：</strong>检查网络级错误码。如果错误是在网络层发生的（连接超时、连接被重置），错误对象中会有 <code>ETIMEDOUT</code>、<code>ECONNRESET</code> 这样的系统错误码。</p>\n<p><strong>第五层：</strong>兜底策略。如果前四层都没命中，就递归检查 error.cause 链（有些 Provider 会把原始错误嵌套在 cause 中），然后进行超时启发式检测，最后做错误消息的文本匹配。</p>\n<p>在五层检测中，超时检测逻辑更加复杂，我们这里详细展开。因为 <code>AbortError</code> 有两种截然不同的含义，它可能是用户主动取消（比如点了“停止生成”按钮），也可能是超时导致的自动取消。这两种情况需要完全不同的处理：</p>\n<ul>\n<li>用户主动取消 → 不应该 fallback，尊重用户意图</li>\n<li>超时自动取消 → 应该 fallback，尝试恢复</li>\n</ul>\n<p>OpenClaw中区分两种情况详细逻辑如下：</p>\n<pre><code class=\"language-plain\">function hasTimeoutHint(err: unknown): boolean {\n &nbsp;if (!err) return false;\n &nbsp;if (readErrorName(err) === \"TimeoutError\") return true;\n &nbsp;const message = getErrorMessage(err);\n &nbsp;return Boolean(message &amp;&amp; isTimeoutErrorMessage(message));\n}\n​\nexport function isTimeoutError(err: unknown): boolean {\n &nbsp;// 检查 1: 直接的超时迹象\n &nbsp;if (hasTimeoutHint(err)) return true;\n &nbsp;if (!err || typeof err !== \"object\") return false;\n &nbsp;\n &nbsp;// 检查 2: 只对 AbortError 继续深入检查\n &nbsp;if (readErrorName(err) !== \"AbortError\") return false;\n &nbsp;\n &nbsp;// AbortError 可能是超时导致的 abort\n &nbsp;const message = getErrorMessage(err);\n &nbsp;if (message &amp;&amp; /request was aborted|request aborted/i.test(message)) {\n &nbsp; &nbsp;return true;\n  }\n &nbsp;\n &nbsp;// 检查 3: 深入 cause/reason 链\n &nbsp;const cause = \"cause\" in err ? err.cause : undefined;\n &nbsp;const reason = \"reason\" in err ? err.reason : undefined;\n &nbsp;return hasTimeoutHint(cause) || hasTimeoutHint(reason);\n}\n</code></pre>\n<p>首先，检查错误本身是否有明显的超时特征（错误名是 TimeoutError，或者消息中包含超时相关的关键词）。</p>\n<p>然后，对于 AbortError，进一步检查消息内容。如果消息中包含 request was aborted 这样的文本，说明是自动取消而非用户主动取消。</p>\n<p>最后，递归检查 cause 和 reason 属性。有些错误会把真正的超时错误嵌套在这些属性中。</p>\n<p>这就是真实工程中的复杂性，同一个错误类型在不同语境下含义完全不同，需要多层检查才能准确判断。</p>\n<p>理解了错误分类后，下一个关键问题是：当需要 Fallback 时，有哪些候选模型可以切换？</p>\n<h2>Fallback 候选收集与 Fallback 链执行</h2>\n<p>Fallback候选模型的主要来源有三个：</p>\n<ol>\n<li>当前正在使用的模型（作为第一候选，因为可能只是临时故障）</li>\n<li>配置的 fallback 列表（比如 <code>[\"openai/gpt-4o\", \"google/gemini-2.0-flash\"]</code>）</li>\n<li>配置的 primary 模型（作为最后兜底）</li>\n</ol>\n<p>但直接把这些来源拼接起来还不够，因为可能存在重复。如果用户配置了 <code>fallbacks: [\"openai/gpt-4o\", \"openai/gpt-4o\"]</code>（手误重复了），或者主模型本身就是 fallback 列表中的一个，不去重就会浪费宝贵的重试次数。OpenClaw中通过<strong>去重收集器</strong>来实现，详细逻辑如下：</p>\n<pre><code class=\"language-plain\">function createModelCandidateCollector(allowlist) {\n &nbsp;const seen = new Set&lt;string&gt;();\n &nbsp;const candidates: ModelCandidate[] = [];\n​\n &nbsp;const addCandidate = (candidate, enforceAllowlist) =&gt; {\n &nbsp; &nbsp;// 生成唯一键：\"provider:model\"\n &nbsp; &nbsp;const key = modelKey(candidate.provider, candidate.model);\n &nbsp; &nbsp;\n &nbsp; &nbsp;// 去重检查\n &nbsp; &nbsp;if (seen.has(key)) return;\n &nbsp; &nbsp;\n &nbsp; &nbsp;// 白名单过滤\n &nbsp; &nbsp;if (enforceAllowlist &amp;&amp; allowlist &amp;&amp; !allowlist.has(key)) return;\n &nbsp; &nbsp;\n &nbsp; &nbsp;seen.add(key);\n &nbsp; &nbsp;candidates.push(candidate);\n  };\n &nbsp;\n &nbsp;return { addCandidate, getCandidates: () =&gt; candidates };\n}\n</code></pre>\n<p>这里有两个关键机制：</p>\n<ul>\n<li>\n<p><strong>去重机制</strong>：使用 <code>\"provider:model\"</code> 作为唯一键，存入 Set 中。相同的 provider + model 组合只会被添加一次。</p>\n</li>\n<li>\n<p><strong>白名单过滤</strong>：不是所有模型都适合作为 fallback。比如你的 Agent 是为 Claude 的风格优化的，可能不希望它 fallback 到一个完全不同风格的模型。白名单机制让你可以精确控制哪些模型可以被纳入候选。</p>\n</li>\n</ul>\n<p>在实际开发中，我们的模型其实经常来自不同的Provider，OpenClaw这里有一个值得深入理解的设计：<strong>跨 Provider 的智能 fallback 链策略</strong>。假设用户配置的 primary 模型是 <code>anthropic/claude</code>，fallback 列表是 <code>[\"openai/gpt-4o\", \"google/gemini\"]</code>。但实际运行时，用户手动选择了 <code>openai/gpt-4o</code> 作为当前模型。这时候应该使用配置的 fallback 列表吗？</p>\n<p>OpenClaw 的逻辑是：<strong>只在当前模型已在 fallback 链中时才使用配置的 fallbacks</strong>。如果用户手动选了一个完全不在配置链中的模型，说明用户有特殊意图，不应该自作主张切到配置的其他模型。这个逻辑防止了“用户明确选择了模型 A，系统却自作主张切到了模型 B”的糟糕体验。</p>\n<p>所有准备工作就绪后，让我们看看 Fallback 链是如何实际执行的：</p>\n<p><img src=\"./images/968763_4.png\" alt=\"\"></p>\n<p>这个流程中OpenClaw在各种边界条件下有四个设计亮点：</p>\n<p><strong>决策一：上下文溢出不触发 Fallback。</strong>前面我们已经解释过原因，换模型可能上下文窗口更小，是饮鸩止渴。</p>\n<p><strong>决策二：用户主动取消立即中止。</strong>用户取消就是用户取消，不应该被解读为“这个模型有问题，换一个试试”。但注意，超时导致的 AbortError 应该继续 fallback。</p>\n<p><strong>决策三：非 FailoverError 也继续尝试。</strong>如果遇到一个无法分类的未知错误，只要还有候选模型，就继续尝试，宁可多试一个也不要提前放弃。</p>\n<p><strong>决策四：失败汇总报告。</strong>当所有候选都失败后，系统会生成一条结构化的错误消息，如 <code>\"All models failed (3): anthropic/claude: rate_limit | openai/gpt-4o: auth | google/gemini: timeout\"</code>。这让用户和开发者一眼就能看出是哪些模型、因为什么原因失败的。比一条泛泛的“服务不可用”有用得多。</p>\n<h2>总结</h2>\n<p>今天我们针对OpenClaw如何处理故障，从三级转移架构、错误分类、Fallback 候选如何收集和执行三个方面进行了详细讲解，让我们通过一张表回顾一下三级故障转移对照详情：</p>\n<p><img src=\"./images/968763_5.png\" alt=\"图片\"></p>\n<p>OpenClaw故障转移的核心设计原则可以总结为三点：<strong>代价最小优先、精确分类驱动策略选择、上下文溢出与 API 故障的路径隔离</strong>。故障转移目标是让系统在出错时优雅地恢复，让用户感知不到底层的波折。</p>\n<h2>思考题</h2>\n<p>为什么上下文溢出错误不应该触发模型 Fallback？如果触发了会有什么后果？</p>\n<p>欢迎你在留言区与我交流！如果今天的课程让你有所收获，也欢迎转发给有需要的朋友，邀请他来一起学习，我们下节课再见！</p>\n","comments":[]}