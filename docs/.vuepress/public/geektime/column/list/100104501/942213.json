{"id":942213,"title":"31｜案例解析：eBPF 揭秘 GPU，大模型训练与推理的性能诊断","content":"<p>你好，我是倪朋飞。</p>\n<p>上一讲，我们把 eBPF 实践中常见的问题做了一次集中答疑。今天，我们换个角度，聊一个非常热门的话题：<strong>如何使用 eBPF 技术监控和诊断 GPU 性能问题</strong>，特别是在大模型（Large Language Model，简称 LLM）训练与推理场景中。</p>\n<p>不知道你在使用 GPU 时有没有遇到过这样的困境：</p>\n<ul>\n<li>\n<p>H100 显示 80% 利用率，但训练速度比预期慢 40%</p>\n</li>\n<li>\n<p>All-Reduce 操作偶发性超时，找不到根因</p>\n</li>\n<li>\n<p>推理服务 P99 延迟突然飙升，nvidia-smi 看不出问题</p>\n</li>\n</ul>\n<p>这些问题的共同点是：<strong>传统的 GPU 监控工具只能告诉你发生了什么，却无法解释为什么发生</strong>。而 eBPF，正是打开这个黑盒的钥匙。</p>\n<h2>GPU 为什么是黑盒？</h2>\n<p>在深入 eBPF 解决方案之前，我们先来理解一下，为什么 GPU 可观测性如此困难。</p>\n<h3>传统工具的局限</h3>\n<p>如下表格所示，目前业界主流的 GPU 监控工具主要有以下几种：</p>\n<p><img src=\"./images/942213_1.png\" alt=\"图片\"><br>\n让我用一个类比来说明这个问题。nvidia-smi 就像汽车仪表盘上的油量表和速度表，它能告诉你“油快没了”或者“车速 80 公里”，但它无法告诉你“为什么油耗这么高”或者“为什么提速这么慢”。而 Nsight 系列工具虽然能深入分析，但就像把车开进 4S 店做全面检测一样，你需要停下来，而且检测本身会显著影响性能。</p><!-- [[[read_end]]] -->\n<h3>大模型场景的挑战</h3>\n<p>在大模型训练和推理场景中，GPU 可观测性面临更加严峻的挑战：</p>\n<ol>\n<li>\n<p><strong>异步 CUDA API 导致时间信息丢失</strong>：现代 CUDA 应用大量使用异步 API（如 <code>cudaMemcpyAsync()</code>、<code>cudaLaunchKernel()</code> with streams）来最大化硬件利用率。API 调用立即返回，CPU 继续执行，而 GPU 在后台按顺序处理操作。从 CPU 的视角看，所有入队操作在微秒级返回，只有最终的同步点才会阻塞。如果只看 CPU 侧 wall-time，很容易把入队很快误判成 GPU 执行很快。要拿到 GPU 侧真正的执行时间，需要 CUDA Event 或设备侧 profiler（如 Nsight/CUPTI）；而 eBPF 更擅长补齐 Host/驱动侧的关联证据。</p>\n</li>\n<li>\n<p><strong>分布式训练中的 NCCL 尾延迟</strong>：分布式训练依赖 NCCL（NVIDIA Collective Communications Library）进行 All-Reduce 等操作。当某个节点出现延迟时，整个训练都会被拖慢，而问题根因可能在 GPU 之外，比如 NIC 争用、PCIe 压力、CPU 干扰等等。</p>\n</li>\n<li>\n<p><strong>多租户环境的干扰源识别</strong>：在 Kubernetes 集群中，多个推理服务共享 GPU 资源。当性能下降时，很难区分是本服务的问题还是邻居服务的影响。</p>\n</li>\n<li>\n<p><strong>推理服务的 P99 延迟抖动</strong>：推理服务对尾延迟极其敏感。偶发的延迟尖峰可能来自内存分配、kernel 调度、甚至是驱动层的行为。</p>\n</li>\n</ol>\n<h3>GPU 可观测性盲区</h3>\n<p>让我用一个具体的例子来说明 GPU 可观测性的盲区问题。假设你有一个典型的 CUDA 工作流：分配显存 → 数据传输到 GPU → 执行 kernel → 数据传输回 CPU。</p>\n<p><strong>同步模式下</strong>，CPU 侧工具还能通过阻塞 API 的耗时获得一些信息，如下图所示：</p>\n<p><img src=\"./images/942213_2.jpg\" alt=\"图片\"></p>\n<p>不过，即使在同步模式下，115μs 的同步时间也是一个不透明的聚合值，它混合了多个隐藏的 GPU 侧阶段：kernel 启动开销（~5μs）、实际执行（~100μs）、清理（~10μs）。你无法区分性能问题是来自启动开销过大、warp 分歧（warp divergence）、还是内存访问模式不佳。</p>\n<p><strong>异步模式下情况更糟</strong>，如下图所示：</p>\n<p><img src=\"./images/942213_3.jpg\" alt=\"图片\"><br>\n所有计时信息都被压缩成一个 455μs 的聚合值。“瓶颈是内存传输还是 kernel 执行？”这个问题从 CPU 侧已经无法回答。</p>\n<h2>eBPF 如何填补 GPU 可观测性空白？</h2>\n<p>eBPF 的核心优势在于：零代码修改、低开销、可在生产环境持续运行。通过多层追踪策略，我们可以覆盖完整的 GPU 软件栈，如下图所示：</p>\n<p><img src=\"./images/942213_4.jpg\" alt=\"图片\"></p>\n<p>从图中可以看出，想要覆盖完整的 GPU 软件栈，需要对从 GPU Hardware 到 PyTorch 应用层代码的每一层都进行观测，并且每一层都有相应的 eBPF 技术支持。具体的跟踪方法和每一层可观测到信息如下表所示：</p>\n<p><img src=\"./images/942213_5.png\" alt=\"图片\"></p>\n<h2>如何追踪 CUDA API？</h2>\n<p>现在，让我们进入动手实践环节，先来看看如何跟踪 CUDA API。GPU 应用主要通过 CUDA 运行时库与 GPU 交互，我们可以使用 uprobe 拦截这些 API 调用。</p>\n<h3>可追踪的关键函数</h3>\n<p>CUDA 的追踪并不需要覆盖所有 CUDA API，在大模型场景下，大多数情况下只需关注最关键的几个即可。我已将最值得追踪的 CUDA API 整理成表格，供你参考：</p>\n<p><img src=\"./images/942213_6.png\" alt=\"图片\"></p>\n<h3>uprobe 追踪 cudaMalloc 示例</h3>\n<p>有了待追踪的具体函数，接下来该如何追踪呢？</p>\n<p>以追踪 <code>cudaMalloc</code> 的调用为例，下面是一个完整的 eBPF 程序。它会捕获每次显存分配的大小、返回地址和状态码。</p>\n<p>首先，我们定义用于用户态和内核态通信的数据结构：</p>\n<pre><code class=\"language-cpp\">// cuda_events.h - 事件数据结构\n#define TASK_COMM_LEN 16\n#define MAX_DETAILS_LEN 128\n\nenum cuda_event_type {\n    CUDA_EVENT_MALLOC = 0,\n    CUDA_EVENT_FREE,\n    CUDA_EVENT_MEMCPY,\n    CUDA_EVENT_LAUNCH_KERNEL,\n    CUDA_EVENT_STREAM_SYNC,\n};\n\nstruct cuda_malloc_event {\n    __u32 pid;                    // 进程 ID\n    __u64 size;                   // 请求分配的大小\n    void *device_addr;            // 分配到的设备地址\n    __s32 ret;                    // 返回码 (cudaError_t)\n    __u64 start_ns;               // 调用开始时间\n    __u64 end_ns;                 // 调用结束时间\n    char comm[TASK_COMM_LEN];     // 进程名\n};\n</code></pre>\n<p>接下来是 eBPF 程序的核心逻辑：</p>\n<pre><code class=\"language-cpp\">// cuda_events.bpf.c - eBPF 程序\n#include \"vmlinux.h\"\n#include &lt;bpf/bpf_helpers.h&gt;\n#include &lt;bpf/bpf_tracing.h&gt;\n#include \"cuda_events.h\"\n\n// Ring buffer 用于向用户态发送事件\nstruct {\n    __uint(type, BPF_MAP_TYPE_RINGBUF);\n    __uint(max_entries, 256 * 1024);\n} events SEC(\".maps\");\n\n// 用于在 uprobe 和 uretprobe 之间传递参数\nstruct malloc_args {\n    void **dev_ptr;\n    size_t size;\n    __u64 start_ns;\n};\n\nstruct {\n    __uint(type, BPF_MAP_TYPE_HASH);\n    __uint(max_entries, 10240);\n    __type(key, __u32);\n    __type(value, struct malloc_args);\n} malloc_args_map SEC(\".maps\");\n\n// 1) uprobe: 在 cudaMalloc 入口捕获参数\nSEC(\"uprobe/cudaMalloc\")\nint trace_cuda_malloc(struct pt_regs *ctx) {\n    __u32 pid = bpf_get_current_pid_tgid() &gt;&gt; 32;\n\n    // 捕获函数参数\n    // cudaMalloc 签名: cudaError_t cudaMalloc(void **devPtr, size_t size)\n    void **dev_ptr = (void **)PT_REGS_PARM1(ctx);\n    size_t size = (size_t)PT_REGS_PARM2(ctx);\n\n    // 保存参数供 uretprobe 使用\n    struct malloc_args args = {\n        .dev_ptr = dev_ptr,\n        .size = size,\n        .start_ns = bpf_ktime_get_ns(),\n    };\n    bpf_map_update_elem(&amp;malloc_args_map, &amp;pid, &amp;args, BPF_ANY);\n\n    return 0;\n}\n\n// 2) uretprobe: 在 cudaMalloc 返回时捕获结果\nSEC(\"uretprobe/cudaMalloc\")\nint trace_cuda_malloc_ret(struct pt_regs *ctx) {\n    __u32 pid = bpf_get_current_pid_tgid() &gt;&gt; 32;\n\n    // 获取之前保存的参数\n    struct malloc_args *args = bpf_map_lookup_elem(&amp;malloc_args_map, &amp;pid);\n    if (!args)\n        return 0;\n\n    // 从用户空间读取实际分配的设备地址\n    void *device_addr;\n    if (bpf_probe_read_user(&amp;device_addr, sizeof(void *), args-&gt;dev_ptr) &lt; 0)\n        device_addr = NULL;\n\n    // 准备事件数据\n    struct cuda_malloc_event *e;\n    e = bpf_ringbuf_reserve(&amp;events, sizeof(*e), 0);\n    if (!e)\n        return 0;\n\n    e-&gt;pid = pid;\n    e-&gt;size = args-&gt;size;\n    e-&gt;device_addr = device_addr;\n    e-&gt;ret = (int)PT_REGS_RC(ctx);  // cudaError_t 返回值\n    e-&gt;start_ns = args-&gt;start_ns;\n    e-&gt;end_ns = bpf_ktime_get_ns();\n    bpf_get_current_comm(&amp;e-&gt;comm, sizeof(e-&gt;comm));\n\n    // 发送事件到用户态\n    bpf_ringbuf_submit(e, 0);\n\n    // 清理临时数据\n    bpf_map_delete_elem(&amp;malloc_args_map, &amp;pid);\n\n    return 0;\n}\n\nchar LICENSE[] SEC(\"license\") = \"GPL\";\n</code></pre>\n<p>这段代码有几个关键点需要你注意：</p>\n<ul>\n<li>\n<p>第一，<code>uprobe/cudaMalloc</code> 在函数入口触发。此时我们能获取函数参数，但还不知道分配是否成功，因为函数还没执行完。我们把参数保存到一个以 PID 为 key 的 hash map 中。</p>\n</li>\n<li>\n<p>第二，<code>uretprobe/cudaMalloc</code> 在函数返回时触发。此时我们能获取返回值，并且可以通过之前保存的 <code>devPtr</code> 指针读取实际分配到的设备地址。</p>\n</li>\n<li>\n<p>第三，为什么需要 <code>bpf_probe_read_user()</code>？因为 <code>cudaMalloc</code> 的第一个参数是一个指向指针的指针 <code>void **devPtr</code>。调用完成后，实际分配的设备地址被写入 <code>*devPtr</code>。我们需要从用户空间读取这个值。</p>\n</li>\n</ul>\n<p>有了 eBPF 程序之后，你还需要一个用户态程序加载 eBPF 程序并持续读取 events 中的数据，完整的代码可以参考<a href=\"https://github.com/feiskyer/ebpf-apps/blob/main/bpf-apps/cuda_events.c\">cuda_events.c</a> 的 Github 代码。为了方便你验证观测的结果，我还写一个用来测试 cudaMalloc 功能的验证代码，你可以在 <a href=\"https://github.com/feiskyer/ebpf-apps/blob/main/bpf-apps/test_cuda_malloc.c\">test_cuda_malloc.c</a> 找到它的完整代码。</p>\n<p>分别编译这两个程序，并在两个不同终端中运行之后，你讲看到类似如下的输出：</p>\n<pre><code class=\"language-plain\"># ./cuda_events\nUsing CUDA runtime library: /usr/lib/x86_64-linux-gnu/libcudart.so\n\nTracing cudaMalloc calls... Hit Ctrl-C to end.\nCOMM             PID     SIZE         DEVICE_ADDR        RESULT          LATENCY\n---------------- ------- ------------ ------------------ --------------- ----------\ntest_cuda_mallo  2908059 1.00 MB      0x7ab21d200000     cudaSuccess     240423.97 us\ntest_cuda_mallo  2908059 1.00 MB      0x7ab21d200000     cudaSuccess     285.03 us\ntest_cuda_mallo  2908059 1.00 MB      0x7ab21d200000     cudaSuccess     216.55 us\n</code></pre>\n<h3>使用 bpftrace 快速验证</h3>\n<p>当然，在开发环境中编写大量用户态和内核态代码可能会显得繁琐。实际上，你还可以使用 bpftrace 快速验证你的想法。比如下面就是一个跟踪几个常见 CUDA 函数的示例：</p>\n<pre><code class=\"language-cpp\">#!/usr/bin/env bpftrace\n// cuda_trace.bt - CUDA API 快速追踪\n\n// 追踪 cudaMalloc 调用\nuprobe:/usr/lib/x86_64-linux-gnu/libcudart.so:cudaMalloc {\n    printf(\"PID %d (%s): cudaMalloc size=%zu bytes\\n\",\n           pid, comm, arg1);\n}\n\n// 追踪 cudaMalloc 返回\nuretprobe:/usr/lib/x86_64-linux-gnu/libcudart.so:cudaMalloc {\n    printf(\"PID %d: cudaMalloc returned %d\\n\", pid, retval);\n}\n\n// 追踪 kernel 启动频率\nuprobe:/usr/lib/x86_64-linux-gnu/libcudart.so:cudaLaunchKernel {\n    @launches[pid, comm] = count();\n}\n\n// 追踪 cudaFree\nuprobe:/usr/lib/x86_64-linux-gnu/libcudart.so:cudaFree {\n    printf(\"PID %d: cudaFree ptr=%p\\n\", pid, arg0);\n}\n\n// 统计 cudaMemcpy 方向\nuprobe:/usr/lib/x86_64-linux-gnu/libcudart.so:cudaMemcpy {\n    // kind: 1=H2D, 2=D2H, 3=D2D\n    @memcpy_kind[arg3] = count();\n    @memcpy_bytes[arg3] = sum(arg2);\n}\n\nEND {\n    print(@launches);\n    print(@memcpy_kind);\n    print(@memcpy_bytes);\n}\n</code></pre>\n<p>执行下面的命令，使用 bpftrace 运行这个脚本：</p>\n<pre><code class=\"language-bash\">sudo bpftrace cuda_trace.bt\n</code></pre>\n<p>以下是一个输出的示例：</p>\n<pre><code class=\"language-plain\">PID 12345 (python): cudaMalloc size=1073741824 bytes\nPID 12345: cudaMalloc returned 0\nPID 12345 (python): cudaMalloc size=1073741824 bytes\nPID 12345: cudaMalloc returned 0\n...\n\n@launches[12345, python]: 45000\n@memcpy_kind[1]: 15000    // H2D\n@memcpy_kind[2]: 15000    // D2H\n@memcpy_bytes[1]: 15000000000\n@memcpy_bytes[2]: 15000000000\n</code></pre>\n<h3>如何定位 libcudart 路径和符号名？</h3>\n<p>有了跟踪程序之后，是否放到生产环境就可以直接运行了呢？答案是并不一定。</p>\n<p>不同环境下 CUDA 库的路径可能不同，符号名也可能因版本而异，这其实是 uprobe 的一个通用问题。所以，在编写 uprobe 之前，我建议你首先确认以下两个信息：</p>\n<p><strong>第一个是查找 libcudart 路径</strong>，有三种常用的方法如下所示：</p>\n<pre><code class=\"language-bash\"># 方法1：查看进程加载的库\nlsof -p &lt;PID&gt; | grep libcudart\n\n# 方法2：使用 ldconfig 查找系统库\nldconfig -p | grep libcudart\n\n# 方法3：在常见路径中查找\nfind /usr/local/cuda* /opt/cuda* -name \"libcudart.so*\" 2&gt;/dev/null\n</code></pre>\n<p><strong>第二个是查看可用的符号名</strong>，可以使用 nm 来查找：</p>\n<pre><code class=\"language-bash\"># 列出 libcudart 的导出符号\nnm -D /usr/lib/x86_64-linux-gnu/libcudart.so | grep -E \"cuda(Malloc|Free|Memcpy|Launch)\"\n\n# 示例输出：\n# 0000000000045678 T cudaFree\n# 0000000000045abc T cudaLaunchKernel\n# 0000000000045def T cudaMalloc\n# 0000000000046012 T cudaMemcpy\n# 0000000000046345 T cudaMemcpyAsync\n</code></pre>\n<p>如果你在容器环境中追踪，需要特别注意：容器内的库路径（如 <code>/usr/local/cuda/lib64/libcudart.so</code>）与宿主机可能不同。一般需要在运行命令里显式指定库路径或做路径映射。</p>\n<h2>如何追踪 NVIDIA 驱动层？</h2>\n<p>掌握了 CUDA API 追踪之后，我们再往下走一层，看看如何追踪 NVIDIA 驱动。你可能会问：已经能追踪 CUDA API 了，为什么还要深入到驱动层？</p>\n<p>原因很简单：CUDA API 追踪提供的是应用层视角，但有些问题的根因藏在更底层。所有用户态的 CUDA 调用，最终都会通过系统的 <code>ioctl</code> 接口与 NVIDIA 内核驱动通信。当你遇到 GPU 硬件报错（比如 Xid 错误码）、显存 ECC 错误、GPU 挂起，或者驱动命令处理延迟、中断响应异常、多 GPU 环境下的 PCIe 通信故障时，光看 CUDA API 是定位不到问题的。</p>\n<p>这里有个麻烦：NVIDIA 驱动是闭源的，它不像 AMD/Intel 开源驱动那样暴露完整的 tracepoint。这就需要我们使用 kprobe 直接 hook 驱动函数。在使用时需要注意，这些内核函数名可能会随驱动版本变化，所以在不同环境下使用时需要先用 <code>sudo bpftrace -l 'kprobe:nvidia_*'</code> 确认函数是否存在。</p>\n<p>那么，驱动层有哪些关键函数值得追踪呢？我整理了一个表格供你参考：</p>\n<p><img src=\"./images/942213_7.png\" alt=\"图片\"></p>\n<blockquote>\n<p>补充说明：如果驱动使用 MSI-X，中断路径可能走 <code>nvidia_isr_msix</code> / <code>nvidia_isr_msix_kthread_bh</code>；如果关注统一内存迁移/缺页，建议在 <code>nvidia-uvm</code> 模块中额外关注 <code>uvm_*</code> 相关函数。</p>\n</blockquote>\n<p>有了这些函数，我们来看一个具体的 bpftrace 示例。</p>\n<p>这里先解释一下 ISR（Interrupt Service Routine，中断服务例程）：当 GPU 完成一批计算任务后，会通过硬件中断通知 CPU，而 <code>nvidia_isr</code> 就是驱动中负责响应这个中断的函数。如果 ISR 执行时间过长，意味着驱动无法及时响应 GPU 的完成信号，这往往会导致推理服务的尾延迟升高，或者训练任务出现卡顿。正常情况下，ISR 应该在几十微秒内完成，超过 100μs 通常意味着驱动层存在瓶颈，值得关注。</p>\n<p>下面这个脚本会追踪 ioctl 调用频率（用于识别高频命令模式），并测量 ISR 的执行延迟。当延迟超过 100μs 时，脚本会打印告警信息：</p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bpftrace\n// nvidia_driver.bt - NVIDIA 驱动层追踪\n\n// 追踪 ioctl 调用频率（识别高频命令模式）\nkprobe:nvidia_unlocked_ioctl {\n    @ioctl_count[pid, comm] = count();\n}\n\n// 测量中断服务延迟\nkprobe:nvidia_isr {\n    @isr_start[tid] = nsecs;\n}\n\nkretprobe:nvidia_isr {\n    $start = @isr_start[tid];\n    if ($start &gt; 0) {\n        &lt;!--§§MATH_0§§--&gt;start;\n        @isr_latency_ns = hist($latency);\n\n        // 告警：ISR 延迟超过 100μs\n        if ($latency &gt; 100000) {\n            printf(\"WARNING: High ISR latency %llu ns (elapsed: %llu s)\\n\",\n                   $latency, elapsed / 1000000000);\n        }\n    }\n    delete(@isr_start[tid]);\n}\n\ninterval:s:10 {\n    print(@ioctl_count);\n    print(@isr_latency_ns);\n    clear(@ioctl_count);\n    clear(@isr_latency_ns);\n}\n\nEND {\n    clear(@isr_start);\n}\n</code></pre>\n<p>正常情况下，你会看到类似如下输出的统计信息，即没有告警信息（有告警信息说明真的出问题了）：</p>\n<pre><code class=\"language-plain\"># bpftrace nvidia_driver.bt\nAttaching 5 probes...\n@ioctl_count[36321, python3.10]: 92\n@ioctl_count[36321, python3.10]: 92\n@ioctl_count[36321, python3.10]: 46\n...\n</code></pre>\n<h2>如何诊断分布式训练的尾延迟？</h2>\n<p>掌握了驱动层追踪之后，我们来看一个更复杂的场景：分布式训练中的尾延迟问题。</p>\n<p>你可能在实际工作中遇到过这样的情况：在多机多卡训练时，明明每个节点的 GPU 利用率都显示正常，但训练速度却比预期慢很多。问题的根源往往是<strong>尾延迟</strong>——某个节点偶发性地变慢，导致其他所有节点都在等它。由于分布式训练依赖 NCCL（NVIDIA Collective Communications Library）进行 All-Reduce 等集合通信操作，任何一个节点的延迟都会拖慢整体进度。</p>\n<p>那么，尾延迟的根因到底在哪里？这里有个关键诊断点：<strong>问题往往不在 GPU 本身，而是在 Host 侧</strong>。网卡争用、CPU 调度干扰、磁盘 I/O 压力，甚至 GPU 过热降频，都可能导致某个节点的 NCCL 操作变慢。而传统的 GPU 监控工具（如 nvidia-smi、DCGM）只关注 GPU 本身，无法捕捉这些 Host 侧的干扰源。</p>\n<h3>基于 eBPF 的 Host-Side Telemetry</h3>\n<p>针对这个问题，最近有研究团队提出了一种基于 eBPF 的 Host-Side Telemetry 方案（论文：arxiv.org/abs/2510.16946）。这个方案的核心思路是：<strong>同时采集多个系统指标，然后通过相关性分析找出导致 NCCL 延迟的根因</strong>。</p>\n<p>具体来说，方案会在 Host 侧部署多个 eBPF 探针，分别采集网络软中断、CPU 上下文切换、磁盘 I/O 请求等指标。当检测到 NCCL 延迟异常时，通过 lagged cross-correlation（滞后互相关）算法，分析哪个指标的异常与 NCCL 延迟最相关。</p>\n<p><img src=\"./images/942213_8.jpg\" alt=\"图片\"><br>\n根据论文的实验结果，这个方案在定位尾延迟根因时达到了 81%～88% 的准确率，而 CPU 开销仅约 1.21%，完全可以在生产环境中持续运行。</p>\n<p>常见的干扰源包括 NIC 争用、CPU 调度干扰、磁盘 I/O 压力和 GPU 降频等，每种都有对应的 eBPF 信号可以捕捉。下面我们以最常见的 NIC 争用为例，看看如何用 bpftrace 快速定位问题。</p>\n<h3>实战：定位 NIC 争用</h3>\n<p>下面是一个 bpftrace 脚本示例，它会同时采集网络软中断、CPU 上下文切换和 TCP 接收等指标。当 NCCL 出现延迟时，你可以用这个脚本快速判断是否存在网络争用：</p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bpftrace\n// nccl_interference.bt - NCCL 干扰源定位\n\n// 追踪网络软中断频率\n// NET_RX 的 softirq 向量号是 3，频率升高通常意味着网卡处理压力大\ntracepoint:irq:softirq_entry /args-&gt;vec == 3/ {\n    @net_rx_count++;\n    @net_rx_time[cpu] = nsecs;\n}\n\n// 追踪 CPU 上下文切换\n// 如果 NCCL 使用的 CPU 核心上有频繁的上下文切换，可能导致通信延迟\ntracepoint:sched:sched_switch {\n    @ctx_switch++;\n}\n\n// 追踪 TCP 接收\n// NCCL 底层走 TCP/IP 时，这个指标反映了网络数据包的接收情况\nkprobe:tcp_v4_rcv {\n    @tcp_recv++;\n}\n\n// 每秒输出一次统计信息\ninterval:s:1 {\n    printf(\"NET_RX: %lld, CTX_SWITCH: %lld, TCP_RECV: %lld\\n\",\n           @net_rx_count, @ctx_switch, @tcp_recv);\n    clear(@net_rx_count);\n    clear(@ctx_switch);\n    clear(@tcp_recv);\n}\n</code></pre>\n<p>运行这个脚本后，如果你看到 NET_RX 和 CTX_SWITCH 在 NCCL 延迟期间明显升高，就可以进一步用 <code>ss</code> 或 <code>netstat</code> 等工具检查是哪个进程在占用网络带宽。</p>\n<p>需要注意的是，这个脚本只是一个快速诊断的起点。在生产环境中，你可能还需要结合 NCCL 自身的调试日志（设置 <code>NCCL_DEBUG=INFO</code>）以及网络监控工具（如 <code>iftop</code>）来做更深入的分析。</p>\n<h2>如何在 GPU 内部运行 eBPF？</h2>\n<p>掌握了 Host 侧的 CUDA API 追踪和驱动层追踪之后，你可能会问：能不能更进一步，直接看到 GPU 内部发生了什么？比如 warp 分歧（warp divergence）有多严重、内存访问模式是否高效、SM 利用率分布情况等。</p>\n<p>这些问题用 Host 侧的 uprobe 和 kprobe 是回答不了的，因为它们只能看到 CPU 发出了什么命令，看不到 GPU 内部怎么执行。传统的做法是用 Nsight Compute 这类工具做离线分析，但开销太大，不适合生产环境。</p>\n<p>最近，开源社区出现了一些新的尝试：把 eBPF 程序直接运行在 GPU 上。这个想法听起来有点偏门，但确实有人在做，而且已经有了可用的原型。</p>\n<h3>bpftime GPU：把 eBPF 带到 GPU 上</h3>\n<p>bpftime 是一个用户态的 eBPF 运行时项目，它最近扩展了 GPU 支持。核心思路是把 eBPF 程序编译成 PTX（Parallel Thread Execution，NVIDIA GPU 的中间指令格式），然后注入到 CUDA kernel 中运行。</p>\n<p>你可能会问：这跟 Linux 内核的 eBPF 是一回事吗？严格来说不是。bpftime GPU 借用了 eBPF 的编程模型和工具链（比如你可以用熟悉的 BPF 语法写程序），但它运行在 GPU 上，helper 函数、map 类型、输出通道都跟内核 eBPF 不同。可以理解为 eBPF 风格的 GPU 追踪。</p>\n<p>下图展示了 bpftime GPU 的基本架构：</p>\n<p><img src=\"./images/942213_9.jpg\" alt=\"图片\"></p>\n<p>在 GPU 侧，bpftime 提供了一组专门的 helper 函数，让你可以获取 GPU 特有的信息。我整理了最常用的几个：</p>\n<p><img src=\"./images/942213_10.png\" alt=\"图片\"><br>\n让我用一个具体的例子来说明 bpftime GPU 能做什么。</p>\n<p>假设你想知道 CUDA kernel 中每个线程的完成时间，以便发现 warp 分歧问题。下面是一个追踪程序的示例：</p>\n<pre><code class=\"language-cpp\">// kernelretsnoop.bpf.c - 追踪 GPU 线程执行时间\nSEC(\"kretprobe/_Z9vectorAddPKfS0_Pf\")\nint ret__cuda() {\n    // 获取当前线程的索引（我是哪个线程？）\n    __u64 tid_x, tid_y, tid_z;\n    bpf_get_thread_idx(&amp;tid_x, &amp;tid_y, &amp;tid_z);\n\n    // 获取当前时间戳（我何时完成？）\n    __u64 ts = bpf_get_globaltimer();\n\n    // 构造事件数据\n    struct thread_exit_event data = {\n        .tid_x = tid_x,\n        .tid_y = tid_y,\n        .tid_z = tid_z,\n        .timestamp = ts,\n    };\n\n    // 写入 ring buffer 供用户态分析\n    bpf_perf_event_output(ctx, &amp;events, 0, &amp;data, sizeof(data));\n    return 0;\n}\n</code></pre>\n<p>这段代码有几个关键点：</p>\n<ul>\n<li>\n<p>第一，<code>SEC(\"kretprobe/_Z9vectorAddPKfS0_Pf\")</code> 表示在 <code>vectorAdd</code> 这个 CUDA kernel 返回时触发。注意这里的函数名是 C++ mangled 之后的名字。</p>\n</li>\n<li>\n<p>第二，<code>bpf_get_thread_idx()</code> 获取当前线程在 block 内的索引。CUDA 的线程组织是三维的（x, y, z），所以有三个返回值。</p>\n</li>\n<li>\n<p>第三，<code>bpf_get_globaltimer()</code> 返回 GPU 全局计时器的值，精度是纳秒级。</p>\n</li>\n</ul>\n<p>有了这些数据，你可以分析 warp 分歧问题。举个例子：如果一个 warp 中的 31 个线程都在 T+100ns 完成，但第 32 个线程在 T+850ns 才完成，这说明存在严重的控制流分歧：某个 if 分支导致一个线程走了很长的路径，而其他线程都在等它。</p>\n<p>如果你对 bpftime GPU 感兴趣，可以参考<a href=\"https://eunomia.dev/bpftime/\">官方文档</a>，了解详细的使用方法和更多示例。需要提醒你的是，bpftime GPU 目前还处于实验阶段，并不适合生产环境。但作为一种新的思路，它值得关注。</p>\n<h2>小结</h2>\n<p>今天，我带你一起探索了如何使用 eBPF 技术来诊断 GPU 性能问题，特别是在大模型训练与推理场景中那些 GPU 利用率看起来正常，但性能就是上不去的疑难杂症。</p>\n<p>我们首先分析了 GPU 可观测性的困境：nvidia-smi 只能告诉你发生了什么，却无法解释为什么发生；而 Nsight 等专业工具虽然能深入分析，但开销太大，不适合生产环境。eBPF 正好填补了这个空白，它可以在生产环境持续运行，开销通常低于 5%。</p>\n<p>接下来，我们从三个层次学习了 eBPF 追踪 GPU 的方法：</p>\n<ul>\n<li>\n<p><strong>CUDA API 层</strong>：使用 uprobe 追踪 <code>cudaMalloc</code>、<code>cudaLaunchKernel</code> 等关键函数，可以发现内存分配模式、kernel 启动频率等应用层问题。</p>\n</li>\n<li>\n<p><strong>NVIDIA 驱动层</strong>：使用 kprobe 追踪 <code>nvidia_unlocked_ioctl</code>、<code>nvidia_isr</code> 等驱动函数，可以定位驱动瓶颈、中断延迟、Xid 错误等底层问题。</p>\n</li>\n<li>\n<p><strong>Host 系统层</strong>：使用 tracepoint 追踪网络软中断、CPU 调度等系统事件，可以发现分布式训练中的 NIC 争用、CPU 干扰等外部干扰源。</p>\n</li>\n</ul>\n<p>在实际工具选型方面，我建议在快速调试时使用 bpftrace 一行命令验证想法；生产监控则采用基于 libbpf 的专用监控程序，并结合 Prometheus 持续采集数据；需要深度分析时，可配合 Nsight 进行离线 profiling。但工具只是手段，关键在于建立从现象到根因的分析思路：先用低开销的 eBPF 追踪缩小问题范围，再用专业工具深入分析具体瓶颈。</p>\n<h2>思考题</h2>\n<p>最后，我想请你聊一聊：你平时是怎么监控 GPU 的？除了 nvidia-smi，还用过哪些工具或方法？有没有遇到过今天课程讲到的各种问题？</p>\n<p>欢迎在留言区分享你的排查思路和踩过的坑，也欢迎把这节课分享给你的同事、朋友。</p>\n","comments":[]}