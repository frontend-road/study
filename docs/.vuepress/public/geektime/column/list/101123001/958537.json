{"id":958537,"title":"10｜Embedding 存储与优化：向量从哪来？数据怎么存？查询怎么优化？","content":"<p>你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。</p>\n<p>上节课我们深入剖析了混合搜索的加权融合、MMR 去重的贪心迭代以及时间衰减的指数模型——这三大算法共同回答了“搜索结果如何又准、又不重、又够新”的问题。但算法再精妙，也需要坚实的数据基础设施来支撑：<strong>向量从哪里来？数据存储在哪里？查询又如何优化？</strong></p>\n<p>这节课我们将聚焦 Memory 系统的数据层，从 Embedding 多提供者体系、SQLite 六张表设计，到分块策略与查询扩展，带你完整走通数据基础设施的每一个环节。</p>\n<h2>EmbeddingProvider 统一接口：屏蔽底层差异的契约</h2>\n<p>在<a href=\"https://time.geekbang.org/column/article/954978\">第 01 课</a>时我们提到，Memory 系统的处理层负责将文本转化为向量。但现实中，嵌入引擎五花八门——OpenAI 用 REST API，Ollama 走本地 HTTP，node-llama-cpp 直接加载 GGUF 模型文件。如果让上层调用者（MemoryIndexManager）逐一适配，代码会迅速膨胀且难以维护。</p>\n<p>OpenClaw 的解决方案是<strong>统一接口</strong>：所有嵌入引擎都必须实现 <code>EmbeddingProvider</code> 接口，上层只需调用接口方法，无需关心底层实现。</p>\n<h3>接口定义</h3>\n<pre><code class=\"language-plain\">// src/memory/embeddings.ts\nexport type EmbeddingProvider = {\n &nbsp;id: string; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; // ★ 提供者唯一标识\n &nbsp;model: string; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// ★ 模型名称（用于缓存键）\n &nbsp;maxInputTokens?: number; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// 最大输入 tokens（可选）\n &nbsp;embedQuery: (text: string) =&gt; Promise&lt;number[]&gt;; &nbsp; &nbsp; &nbsp;// ★ 单条嵌入（搜索时使用）\n &nbsp;embedBatch: (texts: string[]) =&gt; Promise&lt;number[][]&gt;; // ★ 批量嵌入（索引时使用）\n &nbsp;embedBatchInputs?: (inputs: EmbeddingInput[]) =&gt; Promise&lt;number[][]&gt;; &nbsp;// 多模态扩展\n};\n</code></pre><!-- [[[read_end]]] -->\n<p>这份接口契约有三个关键设计：</p>\n<h3><img src=\"./images/958537_1.png\" alt=\"图片\"></h3>\n<h3>为什么区分 embedQuery 和 embedBatch？</h3>\n<p>你可能会问：单条嵌入不就是批量嵌入的特例吗？确实如此，但区分它们有工程上的考量：</p>\n<ol>\n<li>\n<p><strong>API 计费差异</strong>：部分提供商对单次请求和批量请求有不同的计费策略</p>\n</li>\n<li>\n<p><strong>网络开销</strong>：搜索场景下单条请求更快，索引场景下批量请求更省 RTT（往返时延）</p>\n</li>\n<li>\n<p><strong>错误隔离</strong>：批量请求中一条失败不应影响其他条目的处理</p>\n</li>\n</ol>\n<h2>六种嵌入提供者与 Auto 选择链路</h2>\n<p>OpenClaw 内置了六种 <code>EmbeddingProvider</code> 实现，覆盖从云端 API 到本地离线的全部场景：</p>\n<h3><img src=\"./images/958537_2.png\" alt=\"图片\"></h3>\n<h3>Auto 选择与降级链路</h3>\n<p>当用户配置 <code>embedding.provider: \"auto\"</code> 时，系统会按照预设顺序自动尝试可用的提供者：</p>\n<p><img src=\"./images/958537_3.jpg\" alt=\"图片\"></p>\n<pre><code class=\"language-plain\">// src/memory/embeddings.ts - createEmbeddingProvider\nexport async function createEmbeddingProvider(\n &nbsp;config: EmbeddingConfig\n): Promise&lt;EmbeddingProvider | null&gt; {\n &nbsp;const provider = config.provider ?? \"auto\";\n &nbsp;\n &nbsp;if (provider === \"auto\") {\n &nbsp; &nbsp;// ★ 降级顺序：local → openai → gemini → voyage → mistral\n &nbsp; &nbsp;const tryOrder = [\"local\", \"openai\", \"gemini\", \"voyage\", \"mistral\"] as const;\n &nbsp; &nbsp;\n &nbsp; &nbsp;for (const candidate of tryOrder) {\n &nbsp; &nbsp; &nbsp;try {\n &nbsp; &nbsp; &nbsp; &nbsp;const p = await createSpecificProvider(candidate, config);\n &nbsp; &nbsp; &nbsp; &nbsp;if (p) {\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;console.log(`[Embedding] Auto-selected: ${candidate}`);\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;return p;\n &nbsp; &nbsp; &nbsp;  }\n &nbsp; &nbsp;  } catch (e) {\n &nbsp; &nbsp; &nbsp; &nbsp;console.warn(`[Embedding] ${candidate} unavailable:`, e.message);\n &nbsp; &nbsp; &nbsp; &nbsp;// ★ 继续尝试下一个，不中断\n &nbsp; &nbsp;  }\n &nbsp;  }\n &nbsp; &nbsp;\n &nbsp; &nbsp;// ★ 全部失败，返回 null，上层降级为纯 FTS 模式\n &nbsp; &nbsp;console.warn(\"[Embedding] All providers failed, falling back to FTS-only\");\n &nbsp; &nbsp;return null;\n  }\n &nbsp;\n &nbsp;// 显式指定提供者时，直接创建\n &nbsp;return createSpecificProvider(provider, config);\n}\n</code></pre>\n<p><strong>为什么 Local 排在第一位？</strong>这体现了 OpenClaw 的 Local-First 哲学：优先使用本地资源，减少对外部服务的依赖，同时保护用户隐私。只有当本地模型不可用时，才尝试云端 API。</p>\n<h3>降级的最终兜底：纯 FTS 模式</h3>\n<p>如果所有嵌入提供者都失败（例如离线环境且无本地模型），<code>createEmbeddingProvider</code> 返回 <code>null</code>。此时 MemoryIndexManager 会将 <code>provider</code> 字段置空，搜索时自动退化为<strong>纯 FTS 模式</strong>——只使用 BM25 关键词搜索，不进行向量语义匹配。</p>\n<p>这种“优雅降级”设计确保了系统在任何环境下都能工作，只是搜索质量有所下降。</p>\n<h2>Local 提供者：懒初始化与重入保护</h2>\n<p>Local 提供者使用 <code>node-llama-cpp</code> 加载 GGUF 格式的嵌入模型，这是 OpenClaw 实现完全离线语义搜索的关键。但模型加载是一个<strong>耗时且耗内存</strong>的操作（通常需要 3-10 秒，占用 500MB-2GB 内存），因此必须精心管理。</p>\n<h3>懒初始化设计</h3>\n<p>OpenClaw 采用<strong>懒初始化</strong>策略：只有在第一次真正需要嵌入时才加载模型，而非应用启动时立即加载。</p>\n<pre><code class=\"language-plain\">// src/memory/embeddings.ts - createLocalEmbeddingProvider\nlet localInstance: EmbeddingProvider | null = null;\nlet localInitPromise: Promise&lt;EmbeddingProvider | null&gt; | null = null;\n​\nexport async function createLocalEmbeddingProvider(\n &nbsp;config: LocalEmbeddingConfig\n): Promise&lt;EmbeddingProvider | null&gt; {\n &nbsp;// ★ 重入保护：如果已有实例，直接返回\n &nbsp;if (localInstance) return localInstance;\n &nbsp;\n &nbsp;// ★ 并发保护：如果正在初始化，等待现有 Promise\n &nbsp;if (localInitPromise) return localInitPromise;\n &nbsp;\n &nbsp;// ★ 首次初始化：创建 Promise 并缓存\n &nbsp;localInitPromise = (async () =&gt; {\n &nbsp; &nbsp;try {\n &nbsp; &nbsp; &nbsp;const { getLlama } = await import(\"node-llama-cpp\");\n &nbsp; &nbsp; &nbsp;const llama = await getLlama();\n &nbsp; &nbsp; &nbsp;\n &nbsp; &nbsp; &nbsp;// 加载模型文件\n &nbsp; &nbsp; &nbsp;const model = await llama.loadModel({\n &nbsp; &nbsp; &nbsp; &nbsp;modelPath: config.modelPath,\n &nbsp; &nbsp; &nbsp; &nbsp;gpuLayers: config.gpuLayers ?? 0, &nbsp;// 默认 CPU 推理\n &nbsp; &nbsp;  });\n &nbsp; &nbsp; &nbsp;\n &nbsp; &nbsp; &nbsp;// 创建嵌入上下文\n &nbsp; &nbsp; &nbsp;const context = await model.createEmbeddingContext();\n &nbsp; &nbsp; &nbsp;\n &nbsp; &nbsp; &nbsp;localInstance = {\n &nbsp; &nbsp; &nbsp; &nbsp;id: \"local\",\n &nbsp; &nbsp; &nbsp; &nbsp;model: path.basename(config.modelPath),\n &nbsp; &nbsp; &nbsp; &nbsp;embedQuery: async (text) =&gt; {\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;const result = await context.getEmbeddingFor(text);\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;return normalizeVector(result.vector); &nbsp;// ★ 归一化处理\n &nbsp; &nbsp; &nbsp;  },\n &nbsp; &nbsp; &nbsp; &nbsp;embedBatch: async (texts) =&gt; {\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;const results = await Promise.all(\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;texts.map(t =&gt; context.getEmbeddingFor(t))\n &nbsp; &nbsp; &nbsp; &nbsp;  );\n &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;return results.map(r =&gt; normalizeVector(r.vector));\n &nbsp; &nbsp; &nbsp;  },\n &nbsp; &nbsp;  };\n &nbsp; &nbsp; &nbsp;\n &nbsp; &nbsp; &nbsp;return localInstance;\n &nbsp;  } catch (e) {\n &nbsp; &nbsp; &nbsp;console.error(\"[Local Embedding] Init failed:\", e);\n &nbsp; &nbsp; &nbsp;return null;\n &nbsp;  } finally {\n &nbsp; &nbsp; &nbsp;localInitPromise = null; &nbsp;// ★ 清理 Promise 缓存\n &nbsp;  }\n  })();\n &nbsp;\n &nbsp;return localInitPromise;\n}\n</code></pre>\n<h3>为什么需要向量归一化？</h3>\n<p>注意代码中的 <code>normalizeVector()</code> 调用。向量归一化（将向量缩放到单位长度）有两个重要作用：</p>\n<ol>\n<li>\n<p><strong>统一相似度计算</strong>：归一化后，余弦相似度等价于点积，计算更快</p>\n</li>\n<li>\n<p><strong>跨模型兼容</strong>：不同模型输出的向量范数不同，归一化后可以使用统一的相似度阈值</p>\n</li>\n</ol>\n<pre><code class=\"language-plain\">function normalizeVector(vec: number[]): number[] {\n &nbsp;const norm = Math.sqrt(vec.reduce((sum, v) =&gt; sum + v * v, 0));\n &nbsp;if (norm === 0) return vec;\n &nbsp;return vec.map(v =&gt; v / norm);\n}\n</code></pre>\n<h2>SQLite 六张表 Schema 设计</h2>\n<p>OpenClaw 选择 SQLite 作为存储引擎，配合 <code>sqlite-vec</code> 扩展实现向量搜索，配合 <code>FTS5</code> 实现全文搜索。整个 Schema 由六张表组成，各司其职。</p>\n<h3>六张表职责一览</h3>\n<h3><img src=\"./images/958537_4.png\" alt=\"图片\"></h3>\n<h3>核心表结构</h3>\n<pre><code class=\"language-plain\">-- src/memory/storage.ts - Schema 定义\n​\n-- 元数据表：追踪索引状态\nCREATE TABLE IF NOT EXISTS meta (\n &nbsp;key &nbsp; TEXT PRIMARY KEY,\n &nbsp;value TEXT\n);\n​\n-- 文件表：增量同步的基础\nCREATE TABLE IF NOT EXISTS files (\n  path &nbsp; &nbsp; &nbsp;TEXT PRIMARY KEY, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- ★ 相对于项目根目录的路径\n &nbsp;hash &nbsp; &nbsp; &nbsp;TEXT NOT NULL, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;-- ★ 内容 SHA-256，用于检测变更\n  mtime &nbsp; &nbsp; INTEGER NOT NULL, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- 修改时间戳\n  size &nbsp; &nbsp; &nbsp;INTEGER NOT NULL &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;-- 文件大小（字节）\n);\n​\n-- 文本块表：核心数据存储\nCREATE TABLE IF NOT EXISTS chunks (\n  id &nbsp; &nbsp; &nbsp; &nbsp; TEXT PRIMARY KEY, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;-- ★ 复合 Hash ID\n  file_path &nbsp;TEXT NOT NULL,\n  content &nbsp; &nbsp;TEXT NOT NULL, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- 原始文本\n  embedding &nbsp;TEXT, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;-- JSON 编码的向量\n  start_line INTEGER,\n  end_line &nbsp; INTEGER,\n &nbsp;hash &nbsp; &nbsp; &nbsp; TEXT NOT NULL, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- 内容 Hash\n &nbsp;FOREIGN KEY (file_path) REFERENCES files(path) ON DELETE CASCADE\n);\n​\n-- 嵌入缓存表：节省 API 调用\nCREATE TABLE IF NOT EXISTS embedding_cache (\n  content_hash TEXT NOT NULL,\n  model &nbsp; &nbsp; &nbsp; &nbsp;TEXT NOT NULL,\n  embedding &nbsp; &nbsp;TEXT NOT NULL, &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- JSON 编码的向量\n  created_at &nbsp; INTEGER DEFAULT (unixepoch()),\n &nbsp;PRIMARY KEY (content_hash, model) &nbsp; &nbsp; -- ★ 内容+模型组合唯一\n);\n​\n-- 向量虚拟表（sqlite-vec）\nCREATE VIRTUAL TABLE IF NOT EXISTS chunks_vec USING vec0(\n  id &nbsp; &nbsp; &nbsp; TEXT PRIMARY KEY,\n  embedding FLOAT[1536] &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- ★ 维度需与模型匹配\n);\n​\n-- 全文索引虚拟表（FTS5）\nCREATE VIRTUAL TABLE IF NOT EXISTS chunks_fts USING fts5(\n  id,\n  content,\n  content='chunks', &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; -- ★ 内容来源于 chunks 表\n  content_rowid='rowid'\n);\n</code></pre>\n<h3>Chunk ID 生成策略</h3>\n<p>Chunk ID 不是简单的自增整数，而是由多个字段组合生成的 Hash：</p>\n<pre><code class=\"language-plain\">// Chunk ID = SHA-256(source:path:startLine:endLine:contentHash:model)\nfunction generateChunkId(params: {\n &nbsp;source: string; &nbsp; &nbsp; &nbsp;// 数据来源（如 \"file\"）\n &nbsp;path: string; &nbsp; &nbsp; &nbsp; &nbsp;// 文件路径\n &nbsp;startLine: number;\n &nbsp;endLine: number;\n &nbsp;contentHash: string; // 内容 Hash\n &nbsp;model: string; &nbsp; &nbsp; &nbsp; // 嵌入模型名\n}): string {\n &nbsp;const input = [\n &nbsp; &nbsp;params.source,\n &nbsp; &nbsp;params.path,\n &nbsp; &nbsp;params.startLine,\n &nbsp; &nbsp;params.endLine,\n &nbsp; &nbsp;params.contentHash,\n &nbsp; &nbsp;params.model,\n  ].join(\":\");\n &nbsp;\n &nbsp;return crypto.createHash(\"sha256\").update(input).digest(\"hex\");\n}\n</code></pre>\n<p>这种设计有三个好处：</p>\n<ol>\n<li>\n<p><strong>天然去重</strong>：相同内容、相同模型生成的 Chunk ID 必然相同</p>\n</li>\n<li>\n<p><strong>模型隔离</strong>：切换模型后，ID 不同，不会误用旧向量</p>\n</li>\n<li>\n<p><strong>可追溯</strong>：从 ID 可以反推出数据来源</p>\n</li>\n</ol>\n<h2>chunkMarkdown 分块算法</h2>\n<p>将长文档切分为适合嵌入的小块是 Memory 系统的关键步骤。块太大会稀释语义，块太小会丢失上下文。OpenClaw 采用<strong>基于行的滑动窗口</strong>策略。</p>\n<h3>分块参数</h3>\n<h3><img src=\"./images/958537_5.png\" alt=\"图片\"></h3>\n<h3>核心实现</h3>\n<pre><code class=\"language-plain\">// src/memory/chunk.ts - chunkMarkdown\nexport function chunkMarkdown(params: {\n &nbsp;content: string;\n &nbsp;filePath: string;\n &nbsp;targetTokens?: number; &nbsp; &nbsp;// 默认 400\n &nbsp;overlapTokens?: number; &nbsp; // 默认 80\n}): Chunk[] {\n &nbsp;const targetChars = (params.targetTokens ?? 400) * 4; &nbsp; // ★ 1 token ≈ 4 chars\n &nbsp;const overlapChars = (params.overlapTokens ?? 80) * 4;\n &nbsp;\n &nbsp;const lines = params.content.split(\"\\n\");\n &nbsp;const chunks: Chunk[] = [];\n &nbsp;\n &nbsp;let currentChunk: string[] = [];\n &nbsp;let currentLength = 0;\n &nbsp;let startLine = 0;\n &nbsp;\n &nbsp;for (let i = 0; i &lt; lines.length; i++) {\n &nbsp; &nbsp;const line = lines[i];\n &nbsp; &nbsp;const lineLength = line.length + 1; &nbsp;// +1 for newline\n &nbsp; &nbsp;\n &nbsp; &nbsp;// 累积行直到达到目标大小\n &nbsp; &nbsp;if (currentLength + lineLength &gt; targetChars &amp;&amp; currentChunk.length &gt; 0) {\n &nbsp; &nbsp; &nbsp;// ★ 生成当前块\n &nbsp; &nbsp; &nbsp;chunks.push({\n &nbsp; &nbsp; &nbsp; &nbsp;content: currentChunk.join(\"\\n\"),\n &nbsp; &nbsp; &nbsp; &nbsp;filePath: params.filePath,\n &nbsp; &nbsp; &nbsp; &nbsp;startLine,\n &nbsp; &nbsp; &nbsp; &nbsp;endLine: i - 1,\n &nbsp; &nbsp;  });\n &nbsp; &nbsp; &nbsp;\n &nbsp; &nbsp; &nbsp;// ★ 重叠窗口：保留尾部内容作为下一块的开头\n &nbsp; &nbsp; &nbsp;const overlapLines: string[] = [];\n &nbsp; &nbsp; &nbsp;let overlapLength = 0;\n &nbsp; &nbsp; &nbsp;for (let j = currentChunk.length - 1; j &gt;= 0 &amp;&amp; overlapLength &lt; overlapChars; j--) {\n &nbsp; &nbsp; &nbsp; &nbsp;overlapLines.unshift(currentChunk[j]);\n &nbsp; &nbsp; &nbsp; &nbsp;overlapLength += currentChunk[j].length + 1;\n &nbsp; &nbsp;  }\n &nbsp; &nbsp; &nbsp;\n &nbsp; &nbsp; &nbsp;currentChunk = overlapLines;\n &nbsp; &nbsp; &nbsp;currentLength = overlapLength;\n &nbsp; &nbsp; &nbsp;startLine = i - overlapLines.length;\n &nbsp;  }\n &nbsp; &nbsp;\n &nbsp; &nbsp;currentChunk.push(line);\n &nbsp; &nbsp;currentLength += lineLength;\n  }\n &nbsp;\n &nbsp;// 处理最后一个块\n &nbsp;if (currentChunk.length &gt; 0) {\n &nbsp; &nbsp;chunks.push({\n &nbsp; &nbsp; &nbsp;content: currentChunk.join(\"\\n\"),\n &nbsp; &nbsp; &nbsp;filePath: params.filePath,\n &nbsp; &nbsp; &nbsp;startLine,\n &nbsp; &nbsp; &nbsp;endLine: lines.length - 1,\n &nbsp;  });\n  }\n &nbsp;\n &nbsp;return chunks;\n}\n</code></pre>\n<h3>为什么需要重叠窗口？</h3>\n<p>假设有一段连续的文本：“我们昨天讨论了项目方案，今天需要确认实施细节。”</p>\n<p>如果在“方案”和“今天”之间硬切，两个块分别包含：</p>\n<ul>\n<li>\n<p>块 A：“我们昨天讨论了项目方案”</p>\n</li>\n<li>\n<p>块 B：“今天需要确认实施细节”</p>\n</li>\n</ul>\n<p>当用户搜索“项目实施”时，两个块都只能部分匹配。但如果使用重叠窗口：</p>\n<ul>\n<li>\n<p>块 A：“我们昨天讨论了项目方案，今天需要”</p>\n</li>\n<li>\n<p>块 B：“项目方案，今天需要确认实施细节”</p>\n</li>\n</ul>\n<p>块 B 就能同时命中“项目”和“实施”，搜索质量显著提升。</p>\n<h2>runSafeReindex：原子交换的四步骤</h2>\n<p>当需要全量重建索引（例如切换嵌入模型）时，不能简单地清空数据库重新写入——这会导致重建期间搜索完全不可用。OpenClaw 采用<strong>原子交换</strong>策略，确保重建期间旧索引依然可用。</p>\n<h3>四步骤流程</h3>\n<pre><code class=\"language-plain\">// src/memory/storage.ts - runSafeReindex\nexport async function runSafeReindex(params: {\n &nbsp;dbPath: string;\n &nbsp;indexFn: (tempDb: Database) =&gt; Promise&lt;void&gt;;\n}): Promise&lt;void&gt; {\n &nbsp;const tempPath = `${params.dbPath}.tmp-${crypto.randomUUID()}`;\n &nbsp;const backupPath = `${params.dbPath}.backup`;\n &nbsp;\n &nbsp;try {\n &nbsp; &nbsp;// ★ 步骤 1：创建临时数据库\n &nbsp; &nbsp;const tempDb = new Database(tempPath);\n &nbsp; &nbsp;initSchema(tempDb);\n &nbsp; &nbsp;\n &nbsp; &nbsp;// ★ 步骤 2：迁移嵌入缓存（节省 API 费用）\n &nbsp; &nbsp;const originalDb = new Database(params.dbPath);\n &nbsp; &nbsp;const cacheRows = originalDb.prepare(\n &nbsp; &nbsp; &nbsp;\"SELECT content_hash, model, embedding, created_at FROM embedding_cache\"\n &nbsp;  ).all();\n &nbsp; &nbsp;\n &nbsp; &nbsp;const insertCache = tempDb.prepare(\n &nbsp; &nbsp; &nbsp;\"INSERT OR IGNORE INTO embedding_cache VALUES (?, ?, ?, ?)\"\n &nbsp;  );\n &nbsp; &nbsp;for (const row of cacheRows) {\n &nbsp; &nbsp; &nbsp;insertCache.run(row.content_hash, row.model, row.embedding, row.created_at);\n &nbsp;  }\n &nbsp; &nbsp;originalDb.close();\n &nbsp; &nbsp;\n &nbsp; &nbsp;// ★ 步骤 3：执行索引（在临时库上完成所有分块、嵌入、写入）\n &nbsp; &nbsp;await params.indexFn(tempDb);\n &nbsp; &nbsp;tempDb.close();\n &nbsp; &nbsp;\n &nbsp; &nbsp;// ★ 步骤 4：原子交换\n &nbsp; &nbsp;if (fs.existsSync(params.dbPath)) {\n &nbsp; &nbsp; &nbsp;fs.renameSync(params.dbPath, backupPath); &nbsp;// 原文件 → 备份\n &nbsp;  }\n &nbsp; &nbsp;fs.renameSync(tempPath, params.dbPath); &nbsp; &nbsp; &nbsp;// 临时文件 → 正式文件\n &nbsp; &nbsp;\n &nbsp; &nbsp;// 清理备份\n &nbsp; &nbsp;if (fs.existsSync(backupPath)) {\n &nbsp; &nbsp; &nbsp;fs.unlinkSync(backupPath);\n &nbsp;  }\n &nbsp; &nbsp;\n &nbsp; &nbsp;console.log(\"[Reindex] Atomic swap completed successfully\");\n  } catch (error) {\n &nbsp; &nbsp;// ★ 失败回滚\n &nbsp; &nbsp;if (fs.existsSync(backupPath) &amp;&amp; !fs.existsSync(params.dbPath)) {\n &nbsp; &nbsp; &nbsp;fs.renameSync(backupPath, params.dbPath);\n &nbsp;  }\n &nbsp; &nbsp;if (fs.existsSync(tempPath)) {\n &nbsp; &nbsp; &nbsp;fs.unlinkSync(tempPath);\n &nbsp;  }\n &nbsp; &nbsp;throw error;\n  }\n}\n</code></pre>\n<h3>为什么要迁移嵌入缓存？</h3>\n<p>嵌入缓存表存储了已计算的向量。如果切换模型，旧向量确实无法复用（因为 Chunk ID 包含模型名）。但如果<strong>只是重建索引而不换模型</strong>，迁移缓存可以避免大量重复的 API 调用，节省时间和费用。</p>\n<h2>查询扩展：extractKeywords 的多语言策略</h2>\n<p>全文搜索依赖于关键词匹配，但用户的自然语言查询往往包含大量“噪音”（如停用词、标点）。<code>extractKeywords</code> 负责将模糊查询转化为干净的关键词列表。</p>\n<h3>多语言停用词过滤</h3>\n<pre><code class=\"language-plain\">// src/memory/query.ts - extractKeywords\nconst STOP_WORDS = {\n  en: new Set([\"the\", \"a\", \"an\", \"is\", \"are\", \"was\", \"were\", \"be\", \"been\", \"being\",\n               \"have\", \"has\", \"had\", \"do\", \"does\", \"did\", \"will\", \"would\", \"could\",\n               \"should\", \"may\", \"might\", \"must\", \"shall\", \"can\", \"need\", \"to\", \"of\",\n               \"in\", \"for\", \"on\", \"with\", \"at\", \"by\", \"from\", \"as\", \"into\", \"through\"]),\n  zh: new Set([\"的\", \"了\", \"在\", \"是\", \"我\", \"有\", \"和\", \"就\", \"不\", \"人\", \"都\", \"一\",\n               \"一个\", \"上\", \"也\", \"很\", \"到\", \"说\", \"要\", \"去\", \"你\", \"会\", \"着\", \"没有\",\n               \"看\", \"好\", \"自己\", \"这\"]),\n};\n\nexport function extractKeywords(query: string): string[] {\n  // 分词\n  const tokens = query.match(/[\\p{L}\\p{N}_]+/gu) ?? [];\n  \n  return tokens.filter(token =&gt; {\n    // 过滤停用词\n    if (STOP_WORDS.en.has(token.toLowerCase())) return false;\n    if (STOP_WORDS.zh.has(token)) return false;\n    \n    // 过滤过短的英文词（&lt; 2 字符）\n    if (/^[a-zA-Z]+$/.test(token) &amp;&amp; token.length &lt; 2) return false;\n    \n    // 过滤纯数字\n    if (/^\\d+$/.test(token)) return false;\n    \n    return true;\n  });\n}\n</code></pre>\n<h3>中文 Unigram + Bigram 策略</h3>\n<p>中文没有空格分隔，传统分词工具（如 jieba）在 Node.js 环境下部署困难。OpenClaw 采用轻量级的 <strong>Unigram + Bigram</strong> 策略：</p>\n<pre><code class=\"language-plain\">function tokenizeChinese(text: string): string[] {\n  const chars = [...text].filter(c =&gt; /\\p{Script=Han}/u.test(c));\n  const tokens: string[] = [];\n  \n  // Unigram：单字\n  tokens.push(...chars);\n  \n  // Bigram：双字组合\n  for (let i = 0; i &lt; chars.length - 1; i++) {\n    tokens.push(chars[i] + chars[i + 1]);\n  }\n  \n  return tokens;\n}\n\n// 示例：\"讨论方案\" → [\"讨\", \"论\", \"方\", \"案\", \"讨论\", \"论方\", \"方案\"]\n</code></pre>\n<p>这种策略虽然会产生一些无意义的组合（如“论方”），但通过 FTS5 的 BM25 排序，高频无意义词会被自然降权，而有意义的词组（如“讨论”、“方案”）会获得更高分数。</p>\n<h2>总结</h2>\n<p>完成本课学习，你应该掌握了以下知识点：</p>\n<ol>\n<li>\n<p><strong>EmbeddingProvider 统一接口</strong>：通过 <code>embedQuery</code> 和 <code>embedBatch</code> 两个方法屏蔽底层差异，支持六种嵌入提供者</p>\n</li>\n<li>\n<p><strong>Auto 选择与降级链路</strong>：<code>local → openai → gemini → voyage → mistral</code> 的尝试顺序，最终兜底为纯 FTS 模式</p>\n</li>\n<li>\n<p><strong>Local 懒初始化</strong>：重入保护机制避免重复加载模型，向量归一化确保跨模型兼容</p>\n</li>\n<li>\n<p><strong>SQLite 六张表设计</strong>：<code>meta</code>、<code>files</code>、<code>chunks</code>、<code>embedding_cache</code>、<code>chunks_vec</code>、<code>chunks_fts</code> 各司其职</p>\n</li>\n<li>\n<p><strong>Chunk ID 生成策略</strong>：<code>SHA-256(source:path:line:hash:model)</code> 实现天然去重与模型隔离</p>\n</li>\n<li>\n<p><strong>分块重叠窗口</strong>：80 tokens 重叠确保跨块语义连续性</p>\n</li>\n<li>\n<p><strong>原子交换索引</strong>：四步骤保证重建期间旧索引可用</p>\n</li>\n<li>\n<p><strong>多语言查询扩展</strong>：停用词过滤 + 中文 Bigram 策略</p>\n</li>\n</ol>\n<p>到目前为止，我们已经完整走通了 Memory 系统的<strong>读取链路</strong>（搜索算法 + 数据基础设施）。但还有一个关键问题没有解答：<strong>新的记忆是如何写入系统的？Agent 如何与 Memory 系统集成？</strong></p>\n<p>下节课，我们将打通记忆系统与 Agent Loop 的闭环：</p>\n<ul>\n<li>\n<p>Memory Tool 如何注册为 Agent 可调用的工具？</p>\n</li>\n<li>\n<p>Memory Flush 机制如何实现“静默”的记忆持久化？</p>\n</li>\n<li>\n<p>完整的数据流全景——从用户提问到记忆写入再到下次检索</p>\n</li>\n</ul>\n<h2>思考题</h2>\n<ol>\n<li>\n<p><strong>降级策略优化</strong>：当前 Auto 选择链路是固定顺序。如果要根据“上次成功使用的提供者”动态调整顺序，你会如何修改 <code>createEmbeddingProvider</code> 的实现？</p>\n</li>\n<li>\n<p><strong>分块策略权衡</strong>：假设你的应用场景是法律合同分析，文档中经常有跨多段的条款引用。当前 400 tokens 的分块大小是否合适？你会如何调整参数或改进算法？</p>\n</li>\n<li>\n<p><strong>缓存失效问题</strong>：<code>embedding_cache</code> 表使用 <code>content_hash + model</code> 作为主键。如果 OpenAI 在不改变模型名的情况下更新了嵌入算法（输出向量发生变化），缓存会产生什么问题？你有什么解决方案？</p>\n</li>\n</ol>\n<p>欢迎你在留言区与我交流！如果今天的课程让你有所收获，也欢迎转发给有需要的朋友，邀请他来一起学习，我们下节课再见！</p>\n","comments":[]}