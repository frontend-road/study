{"id":968648,"title":"24｜五层递进式上下文防护：如何应对 Token 爆炸问题？","content":"<p>你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。</p>\n<p>提到上下文压缩，我们过去最注意的是压缩什么时候触发、摘要怎么分块、标识符怎么保留。这节课我们要把视角切换到系统架构师的角度，深入解析OpenClaw从启动前检查、到运行时自动压缩、到溢出恢复、到超大结果截断、再到历史裁剪的完整防御纵深体系。OpenClaw 用五层递进式防护来应对 Token 爆炸问题，每一层都是上一层的兜底，代价逐层递增。</p>\n<p>这里你可能会想，为什么把上下文防护作为“故障转移与高可用”模块的开篇？因为 Token 爆炸本质上就是一种“故障”，当上下文溢出时，LLM API 直接拒绝请求，Agent 瘫痪。上下文防护是 Agent 高可用的第一道防线。只有理解了这道防线，你才能理解为什么故障转移链会特殊处理上下文溢出。</p>\n<h2>Token 爆炸：为什么它是 Agent 的致命威胁</h2>\n<p>上下文窗口有多大？每个大语言模型都有上下文窗口限制，就像一个容量有限的“记忆容器”。</p>\n<p><img src=\"./images/968648_1.png\" alt=\"图片\"></p>\n<p>看起来很大，但在 Agent 场景下，Token 消耗速度比你想象的快得多。一个粗略的测算：</p>\n<ul>\n<li>\n<p>System Prompt：约 2K tokens</p>\n</li>\n<li>\n<p>工具声明（Tools Schema）：约 5K tokens</p>\n</li>\n<li>\n<p>10 轮对话历史：约 20K tokens</p>\n</li>\n<li>\n<p>一次 <code>bash(\"cat large-file.txt\")</code> 的结果：可能 50K+ tokens</p>\n</li>\n<li>\n<p>一次浏览器截图的 base64 编码：可能 100K+ tokens</p>\n</li>\n</ul><!-- [[[read_end]]] -->\n<p>一个 128K 上下文窗口的模型，一条 <code>cat</code> 命令就可能占掉 40% 的容量。如果用户连续执行几次大文件读取，溢出风险就变成了确定的风险。</p>\n<p>当上下文超过模型的窗口限制时，LLM API 不会优雅地截断，<strong>它直接报错拒绝整个请求</strong>。用户看到的是 Agent 突然“死了”，之前的所有对话上下文全部丢失。</p>\n<p>面对这个问题，一个朴素的方案是“在上下文快满的时候做一次压缩”。但如果压缩不及时、压缩失败怎么办？系统需要的不是一道墙，而是多层递进式的一套纵深防御，每一层用不同的策略，解决不同的场景，承担不同的代价。</p>\n<p>下面我们针对OpenClaw的五层纵深防御系统进行详细介绍。</p>\n<h2>五层纵深防护系统</h2>\n<p>在逐层深入之前，让我们先鸟瞰整个防护体系：<br>\n<img src=\"./images/968648_2.png\" alt=\"\"></p>\n<p><strong>关键设计哲学：代价递增。</strong>Layer 1 完全无代价（只是一次检查），Layer 2 温和（丢失一些细节，保留关键信息），Layer 3 更激进（强制压缩），Layer 4 直接“手术刀”截断，Layer 5 直接丢弃历史。每一层都是前一层的兜底，只有当前一层无法解决问题时，才触发下一层。</p>\n<p>它遵循一个通用的工程原则：<strong>在可能的范围内用最小代价解决问题</strong>。你不会一上来就丢弃历史，那是最后的手段。</p>\n<p>理解了全景之后，我们从 Layer 1 开始，逐层深入。你会看到每一层如何用不同的策略解决不同的场景，以及它们之间的“交班逻辑”是如何设计的。</p>\n<h3>Layer 1：Context Window Guard——启动前的第一道防线</h3>\n<p>五层防护中最容易被忽视的其实是第一层，启动前的上下文窗口守卫。它代码量很小，但意义重大：<strong>在 Agent 还没开始工作之前，就拦住那些注定会失败的配置</strong>。</p>\n<p>核心是两个阈值常量：</p>\n<pre><code class=\"language-plain\">// 两个阈值常量\nexport const CONTEXT_WINDOW_HARD_MIN_TOKENS = 16_000; &nbsp; // 硬性最小值\nexport const CONTEXT_WINDOW_WARN_BELOW_TOKENS = 32_000; &nbsp;// 警告阈值\n</code></pre>\n<p>为什么是 16K？测算逻辑是：System Prompt（2K）+ 工具声明（5K）+ 最低限度的对话（几K）就几乎占满了。如果模型的上下文窗口低于 16K tokens，Agent 根本没有足够的空间工作。与其让用户在中途遇到频繁的溢出错误，不如在启动时就明确拒绝。</p>\n<p>不同的模型，上下文窗口大小的来源不同。系统设计了一套上下文窗口的<strong>优先级链</strong>来解析：</p>\n<pre><code class=\"language-plain\">1. modelsConfig — 用户在配置文件中为特定模型配置的 contextWindow（最高优先级）\n2. model &nbsp; &nbsp; &nbsp; — 模型定义自带的 contextWindow\n3. default &nbsp; &nbsp; — 全局默认值 DEFAULT_CONTEXT_TOKENS\n​\n然后检查 agents.defaults.contextTokens 上限：\n  如果配置了且 &lt; 上面解析的值 → 用它作为上限\n</code></pre>\n<p>用户配置覆盖模型默认值，而 <code>contextTokens</code> 作为一个全局上限兜底。有些用户可能在一个团队环境中，需要统一限制所有 Agent 使用的上下文大小以控制成本。</p>\n<p>Layer 1 很轻量，只在启动时做一次检查，不影响运行时性能。但启动之后，对话越来越长，Token 逐渐累积，我们需要运行时的自动管理，下面就是 Layer 2 的工作。</p>\n<h3>Layer 2 与 Layer 5：分阶段摘要算法——压缩的核心引擎</h3>\n<p>Layer 2（Auto-Compaction）和 Layer 5（History Pruning）共用的压缩算法引擎。四个常量直接决定了压缩的行为，详细配置如下：</p>\n<pre><code class=\"language-plain\">export const BASE_CHUNK_RATIO = 0.4; &nbsp; &nbsp;// 压缩分块比率基准：目标是原文的 40%\nexport const MIN_CHUNK_RATIO = 0.15; &nbsp; &nbsp;// 最小压缩比率：最激进可到 15%\nexport const SAFETY_MARGIN = 1.2; &nbsp; &nbsp; &nbsp; // 20% 安全边际\nexport const SUMMARIZATION_OVERHEAD_TOKENS = 4096; &nbsp;// 摘要请求本身的开销预留\n</code></pre>\n<p>其中最关键的是 <code>SAFETY_MARGIN = 1.2</code>。为什么需要 20% 的安全边际？因为 Token 估算用的是 <code>chars / 4</code> 的启发式算法，这对英文文本还算准确，但对中文（约 2-3 字符/Token）、代码（特殊符号多）、多字节字符等场景会低估。20% 的缓冲确保即使估算偏低，也不会在压缩过程中超出上下文窗口。</p>\n<p>不同的会话，消息大小差异巨大。一条“好的”可能只有几个 Token，但一条代码 diff 可能有几千个 Token。所以在压缩过程中，需要自适应不同分块的压缩比率，OpenClaw中 <code>computeAdaptiveChunkRatio()</code> 的解决方案是根据平均消息大小动态调整，详细逻辑如下：</p>\n<pre><code class=\"language-plain\">export function computeAdaptiveChunkRatio(\n &nbsp;messages: AgentMessage[], contextWindow: number\n): number {\n &nbsp;const totalTokens = estimateMessagesTokens(messages);\n &nbsp;const avgTokens = totalTokens / messages.length;\n &nbsp;const safeAvgTokens = avgTokens * SAFETY_MARGIN; &nbsp; &nbsp;// 先乘安全边际\n &nbsp;const avgRatio = safeAvgTokens / contextWindow;\n​\n &nbsp;if (avgRatio &gt; 0.1) { &nbsp; // 平均消息占上下文窗口 &gt; 10%\n &nbsp; &nbsp;const reduction = Math.min(avgRatio * 2, BASE_CHUNK_RATIO - MIN_CHUNK_RATIO);\n &nbsp; &nbsp;return Math.max(MIN_CHUNK_RATIO, BASE_CHUNK_RATIO - reduction);\n  }\n &nbsp;return BASE_CHUNK_RATIO; &nbsp; // 默认 0.4\n}\n</code></pre>\n<p>这个算法的关键是：当 <code>avgRatio &gt; 0.1</code>（平均消息超过上下文窗口的 10%）时，使用 <code>avgRatio × 2</code> 作为衰减量，从 0.4 向 0.15 衰减。当消息特别大时，分块比率会自动降低，确保每个 chunk 都能在摘要请求的上下文窗口内处理。</p>\n<p>OpenClaw中采用链式摘要算法，<code>chunkMessagesByMaxTokens()</code> 负责把消息按 Token 分块，其中 <code>SAFETY_MARGIN</code> 被<strong>第二次使用</strong>——先把 <code>maxTokens</code> 除以 1.2 得到 <code>effectiveMax</code>，再以此为上限分块。这意味着 SAFETY_MARGIN 在两处叠加使用，提供了约 44% 的总体安全缓冲（1.2 × 1.2 ≈ 1.44）。分块完成后，<code>summarizeChunks()</code> 执行<strong>链式摘要</strong>：</p>\n<pre><code class=\"language-plain\">Chunk 1 → retryAsync(generateSummary, 3次) → Summary 1\nChunk 2 + previousSummary=Summary 1 → Summary 2\nChunk 3 + previousSummary=Summary 2 → Summary 3（最终结果）\n</code></pre>\n<p>为什么是链式而非并行？因为后一个 chunk 的摘要需要参考前一个 chunk 的摘要结果，否则整体叙事的连贯性会断裂。每次摘要调用都有 3 次重试（500ms-5000ms 间隔，带 20% jitter），确保单次 API 波动不会导致整个压缩失败。</p>\n<p>但是 LLM 摘要调用可能因限流、超时、安全过滤等原因失败。所以还需要对此进行进一步的防护，OpenClaw中 <code>summarizeWithFallback()</code> 实现了三级降级策略：</p>\n<ul>\n<li>\n<p>Level 1：全量摘要（包含所有消息）</p>\n</li>\n<li>\n<p>Level 2：排除超大消息后重试，如果某条消息超过上下文窗口的 50%（<code>isOversizedForSummary</code>），它会导致摘要请求本身超出窗口。排除这些消息后，附上一条标记：<code>\"[Large assistant (~30K tokens) omitted from summary]\"</code></p>\n</li>\n<li>\n<p>Level 3：纯计数兜底，<code>\"Context contained 50 messages (3 oversized). Summary unavailable due to size limits.\"</code></p>\n</li>\n</ul>\n<p>降级确保系统不会因为摘要失败而卡死，即使只剩下一条“50 条消息、3 条超大”的简短描述，也比 Agent 彻底瘫痪强得多。</p>\n<h3>Layer 3：Overflow Recovery——溢出后的紧急救援</h3>\n<p>自动摘要解决了大部分场景，但如果压缩不及时，LLM API 已经返回了上下文溢出错误怎么办？这就是 Layer 3 的工作，溢出恢复。</p>\n<p>当 API 返回溢出错误时，系统不会立即放弃，而是进入紧急救援模式：</p>\n<ol>\n<li>\n<p>强制触发一次压缩</p>\n</li>\n<li>\n<p>重试 API 调用</p>\n</li>\n<li>\n<p>最多重复 3 次</p>\n</li>\n</ol>\n<p>如果 3 次强制压缩后仍然溢出，说明问题不在历史消息，很可能是单条工具结果就超过了窗口限制，需要触发下一层防护。这个“最多 3 次”的设计体现了一个重要原则：<strong>每一层防护都要有明确的退出条件</strong>，避免无限循环。</p>\n<p>有一种特殊场景Layer 2 和 Layer 3处理不了：<strong>单条工具结果就超过了上下文窗口的合理占比</strong>。那需要Layer 4进行防护。</p>\n<h3>Layer 4：Tool Result Truncation——超大工具结果的手术刀</h3>\n<p>例如这个场景，用户让 Agent 执行 <code>bash(\"cat /var/log/syslog\")</code>，结果返回了 200K 字符。即使把其他所有消息都压缩掉，这一条工具结果仍然撑爆窗口。摘要没法帮忙，因为你不能“摘要”一个系统日志文件，然后期望 Agent 用摘要来排查问题。OpenClaw的解决方案是“手术刀式截断”：只切这一条超大结果，不影响其他任何内容。</p>\n<p>这里截断涉及三个核心阈值：</p>\n<pre><code class=\"language-plain\">const MAX_TOOL_RESULT_CONTEXT_SHARE = 0.3; &nbsp; // 单个结果最多占 30% 上下文\nexport const HARD_MAX_TOOL_RESULT_CHARS = 400_000; &nbsp;// 硬性上限 400K 字符\nconst MIN_KEEP_CHARS = 2_000; &nbsp;// 截断时最少保留 2000 字符\n</code></pre>\n<p>三个阈值的设计逻辑非常清晰：</p>\n<ul>\n<li>\n<p><strong>30%</strong> 上下文占比：给 System Prompt、工具声明、对话历史留出足够空间</p>\n</li>\n<li>\n<p><strong>400K 字符</strong> 硬上限：即使是 2M Token 的超大窗口模型，单条结果也不能超过这个值</p>\n</li>\n<li>\n<p><strong>2000 字符</strong> 最低保留：截断再激进，也要保留足够内容让模型理解“这是什么”</p>\n</li>\n</ul>\n<p>截断不是简单地砍头或砍尾。系统实现了一种 <strong>Head+Tail 智能截断</strong>策略，详细逻辑如下：</p>\n<pre><code class=\"language-plain\">export function truncateToolResultText(text: string, maxChars: number): string {\n &nbsp;if (text.length &lt;= maxChars) return text;\n &nbsp;const budget = Math.max(MIN_KEEP_CHARS, maxChars - TRUNCATION_SUFFIX.length);\n​\n &nbsp;// 如果尾部包含重要内容 → Head 70% + Tail 30%\n &nbsp;if (hasImportantTail(text) &amp;&amp; budget &gt; MIN_KEEP_CHARS * 2) {\n &nbsp; &nbsp;const tailBudget = Math.min(Math.floor(budget * 0.3), 4_000);\n &nbsp; &nbsp;const headBudget = budget - tailBudget - MIDDLE_OMISSION_MARKER.length;\n &nbsp; &nbsp;// 保留开头 + 保留结尾，中间插入省略标记\n &nbsp; &nbsp;return text.slice(0, headCut) + MIDDLE_OMISSION_MARKER + text.slice(tailStart) + suffix;\n  }\n​\n &nbsp;// 默认：只保留开头\n &nbsp;return text.slice(0, budget) + TRUNCATION_SUFFIX;\n}\n</code></pre>\n<p><code>hasImportantTail()</code> 会检测尾部最后 2000 字符是否包含这些模式：</p>\n<ul>\n<li>\n<p>错误信息：<code>error</code>、<code>exception</code>、<code>traceback</code>、<code>panic</code>、<code>exit code</code></p>\n</li>\n<li>\n<p>JSON 闭合：<code>}</code> 结尾（JSON 输出的结构化结束）</p>\n</li>\n<li>\n<p>结果摘要：<code>total</code>、<code>summary</code>、<code>result</code>、<code>complete</code></p>\n</li>\n</ul>\n<p><strong>为什么尾部这么重要？</strong>因为在很多场景下，工具输出的最有价值的信息恰恰在末尾，错误堆栈在末尾，命令执行结果在末尾，JSON 的闭合结构在末尾。如果只保留开头，Agent 会丢失最关键的信息。</p>\n<p>到这里我们已经有了 Auto-Compact、Overflow Recovery、Tool Result Truncation，但是还需要一个<strong>指挥官</strong>来协调这些机制。这就是我们下面讲的压缩安全守卫。</p>\n<h2>压缩安全守卫：把所有层串起来的指挥官</h2>\n<p>压缩安全守卫（compaction-safeguard）接管了压缩事件，在触发压缩之前做一系列预处理和后处理。</p>\n<p>首先是质量审计，限制摘要必须包含什么，质量审计会检查生成的摘要是否包含<strong>五个必需的 section</strong>：</p>\n<pre><code class=\"language-plain\">const REQUIRED_SUMMARY_SECTIONS = [\n &nbsp;\"## Decisions\", &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;// 做过的决策\n &nbsp;\"## Open TODOs\", &nbsp; &nbsp; &nbsp; &nbsp; // 待办事项\n &nbsp;\"## Constraints/Rules\", &nbsp;// 约束条件\n &nbsp;\"## Pending user asks\", &nbsp;// 用户未完成的请求\n &nbsp;\"## Exact identifiers\", &nbsp;// 精确标识符（commit hash、文件路径等）\n];\n</code></pre>\n<p>它们是 Agent 在压缩后“恢复记忆”的关键信息。如果摘要丢失了“之前决定用方案 A”这个信息，Agent 可能会重新提议方案 B，让用户困惑。如果丢失了 commit hash，Agent 就无法引用之前的代码变更。</p>\n<h2>总结</h2>\n<p>我们用一张表总结一下五层防护核心：</p>\n<p><img src=\"./images/968648_3.png\" alt=\"图片\"><br>\n渐进式的上下文防护，核心概括为三个设计原则：</p>\n<ol>\n<li>\n<p><strong>纵深防御：</strong>不依赖单一机制，五层递进，每层兜底</p>\n</li>\n<li>\n<p><strong>代价递增：</strong>从无代价到有代价，从温和到激进</p>\n</li>\n<li>\n<p><strong>自动化运行：</strong>不依赖人工干预，每一层都自动触发</p>\n</li>\n</ol>\n<p>这些原则不仅适用于上下文管理，也是所有复杂系统设计的通用智慧。当你面对任何可能失败的场景时，都可以问自己：我有几层防护？它们的代价是否递增？触发条件是否自动化？</p>\n<h2>思考题</h2>\n<p>如果你要为一个 Token 窗口只有 8K 的本地模型设计防护策略，会做哪些调整？</p>\n<p>欢迎你在留言区与我交流！如果今天的课程让你有所收获，也欢迎转发给有需要的朋友，邀请他来一起学习，我们下节课再见！</p>\n","comments":[]}