{"id":958216,"title":"09｜混合搜索、MMR 去重与时间衰减：如何协作解决“准、不重、够新”？","content":"<p>你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。</p>\n<p>上节课我们走完了 <code>MemoryIndexManager</code> 的 <code>search()</code> 方法八步流程，但在第六步“混合合并”、第七步“后处理”中，我特意用<strong>黑盒</strong>标注了三个关键函数：<code>mergeHybridResults</code>、<code>mmrRerank</code>、<code>applyTemporalDecay</code>。当时我说“先当黑盒用，下节课再打开”——现在，就是打开它们的时候了。</p>\n<p>这三个算法分别解决记忆检索中的三大核心挑战：</p>\n<p><img src=\"./images/958216_1.png\" alt=\"图片\"></p>\n<p>这节课的核心任务，就是把这三个黑盒逐一拆解。</p>\n<h2>混合搜索：向量 + 关键词双引擎</h2>\n<h3>为什么需要混合？</h3>\n<p>单一检索模式各有软肋：</p>\n<ul>\n<li>\n<p><strong>纯向量搜索</strong>：擅长语义理解（如何部署≈怎么上线），但对专有名词、代码片段容易失焦。</p>\n</li>\n<li>\n<p><strong>纯关键词搜索</strong>：精确匹配能力强（搜 <code>MemoryIndexManager</code> 绝不会漏），但无法理解同义表达。</p>\n</li>\n</ul>\n<p>OpenClaw 的解决方案是<strong>双引擎并行 + 加权融合</strong>。默认权重配置：</p>\n<pre><code class=\"language-plain\">向量权重 = 0.7（70%） &nbsp;\n关键词权重 = 0.3（30%）\n</code></pre>\n<p>这个比例是经验值——向量在语义理解上贡献更大，但关键词作为“精确锚点”不可或缺。</p>\n<h3>向量搜索路径</h3>\n<p>向量搜索优先使用 SQLite 扩展 <code>sqlite-vec</code> 提供的 <code>vec_distance_cosine()</code> 函数，直接在数据库层完成近似最近邻检索。如果扩展不可用，则降级为 JavaScript 层计算余弦相似度：</p><!-- [[[read_end]]] -->\n<pre><code class=\"language-plain\">// src/memory/hybrid.ts\n// ★ 向量距离转相似度：distance ∈ [0, 2]，similarity ∈ [0, 1]\nfunction distanceToSimilarity(distance: number): number {\n &nbsp;return 1 - distance / 2;\n}\n</code></pre>\n<h3>关键词搜索与 FTS 查询构建</h3>\n<p>关键词搜索基于 SQLite FTS5 全文索引，使用 <code>MATCH</code> 语法和 <code>bm25()</code> 排名函数。但用户的自然语言查询不能直接扔给 FTS5，需要先构建合法的查询表达式：</p>\n<pre><code class=\"language-plain\">// src/memory/hybrid.ts\nexport function buildFtsQuery(raw: string): string | null {\n &nbsp;// ★ Unicode 感知分词：支持中文、日文等非拉丁字符\n &nbsp;const tokens = raw.match(/[\\p{L}\\p{N}_]+/gu)\n &nbsp;  ?.map(t =&gt; t.trim())\n &nbsp;  .filter(Boolean) ?? [];\n &nbsp;\n &nbsp;if (tokens.length === 0) return null;\n &nbsp;\n &nbsp;// ★ 双引号包裹 + AND 连接，确保精确匹配\n &nbsp;const quoted = tokens.map(t =&gt; `\"${t.replaceAll('\"', \"\")}\"`);\n &nbsp;return quoted.join(\" AND \");\n}\n</code></pre>\n<p><strong>关键设计点</strong>：</p>\n<ol>\n<li>\n<p>使用 <code>\\p{L}\\p{N}</code> Unicode 属性匹配，天然支持多语言分词</p>\n</li>\n<li>\n<p>每个 token 用双引号包裹，防止 FTS5 特殊字符注入</p>\n</li>\n<li>\n<p>使用 <code>AND</code> 连接，要求所有词都出现（提高精确度）</p>\n</li>\n</ol>\n<h2>BM25 分数转换：从负数到归一化</h2>\n<h3>问题背景</h3>\n<p>SQLite FTS5 的 <code>bm25()</code> 函数返回的是<strong>负数</strong>——越小表示越相关（-10 比 -1 更好）。但我们的混合合并需要一个 <code>(0, 1)</code> 区间的分数，方便与向量相似度加权求和。</p>\n<h3>转换公式推导</h3>\n<p>设 <code>rank</code> 为 <code>bm25()</code> 返回值（负数），令 <code>relevance = -rank</code>（正数）。我们使用<strong>双曲变换</strong>将其映射到 <code>(0, 1)</code>：</p>\n<p>$$\\text{score} = \\frac{\\text{relevance}}{1 + \\text{relevance}}$$</p>\n<p>这个公式的优雅之处在于：</p>\n<ul>\n<li>\n<p>当 <code>relevance → 0</code> 时，<code>score → 0</code></p>\n</li>\n<li>\n<p>当 <code>relevance → ∞</code> 时，<code>score → 1</code></p>\n</li>\n<li>\n<p>单调递增，保持排序一致性</p>\n</li>\n</ul>\n<h3>完整代码实现</h3>\n<pre><code class=\"language-plain\">// src/memory/hybrid.ts\nexport function bm25RankToScore(rank: number): number {\n &nbsp;// ★ 防御性处理：非有限数字返回极低分\n &nbsp;if (!Number.isFinite(rank)) {\n &nbsp; &nbsp;return 1 / (1 + 999);\n  }\n &nbsp;\n &nbsp;// ★ 核心转换：负数 rank → (0, 1) 区间\n &nbsp;if (rank &lt; 0) {\n &nbsp; &nbsp;const relevance = -rank;\n &nbsp; &nbsp;return relevance / (1 + relevance);\n  }\n &nbsp;\n &nbsp;// 兜底：正数 rank（理论上不应出现）\n &nbsp;return 1 / (1 + rank);\n}\n</code></pre>\n<p><strong>测试验证</strong>：</p>\n<ul>\n<li>\n<p><code>bm25RankToScore(-10)</code> → <code>10/11 ≈ 0.909</code>（高相关）</p>\n</li>\n<li>\n<p><code>bm25RankToScore(-1)</code> → <code>1/2 = 0.5</code>（中等相关）</p>\n</li>\n<li>\n<p><code>bm25RankToScore(-0.1)</code> → <code>0.1/1.1 ≈ 0.091</code>（低相关）</p>\n</li>\n</ul>\n<h2>混合合并五步流程</h2>\n<h3>流程概览</h3>\n<p><code>mergeHybridResults</code> 是混合搜索的“大脑”，负责将两路搜索结果融合为最终排序。完整流程分为五步：</p>\n<p><img src=\"./images/958216_2.jpg\" alt=\"图片\"></p>\n<h3><img src=\"./images/958216_3.png\" alt=\"图片\"></h3>\n<h3>完整代码实现</h3>\n<pre><code class=\"language-plain\">// src/memory/hybrid.ts\nexport async function mergeHybridResults(params: {\n &nbsp;vector: HybridVectorResult[];\n &nbsp;keyword: HybridKeywordResult[];\n &nbsp;vectorWeight: number; &nbsp; &nbsp;// 默认 0.7\n &nbsp;textWeight: number; &nbsp; &nbsp; &nbsp;// 默认 0.3\n &nbsp;mmr?: Partial&lt;MMRConfig&gt;;\n &nbsp;temporalDecay?: Partial&lt;TemporalDecayConfig&gt;;\n &nbsp;nowMs?: number;\n}): Promise&lt;HybridMergedResult[]&gt; {\n &nbsp;const byId = new Map&lt;string, MergedEntry&gt;();\n​\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;// Step 1: 按 ID 合并两路结果\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;\n &nbsp;// ★ 先处理向量结果\n &nbsp;for (const r of params.vector) {\n &nbsp; &nbsp;byId.set(r.id, {\n &nbsp; &nbsp; &nbsp;...r,\n &nbsp; &nbsp; &nbsp;vectorScore: r.vectorScore,\n &nbsp; &nbsp; &nbsp;textScore: 0, &nbsp;// 占位，等待关键词结果填充\n &nbsp;  });\n  }\n​\n &nbsp;// ★ 再合并关键词结果\n &nbsp;for (const r of params.keyword) {\n &nbsp; &nbsp;const existing = byId.get(r.id);\n &nbsp; &nbsp;if (existing) {\n &nbsp; &nbsp; &nbsp;// 已存在：补充 textScore 和 snippet\n &nbsp; &nbsp; &nbsp;existing.textScore = r.textScore;\n &nbsp; &nbsp; &nbsp;if (r.snippet?.length &gt; 0) {\n &nbsp; &nbsp; &nbsp; &nbsp;existing.snippet = r.snippet;\n &nbsp; &nbsp;  }\n &nbsp;  } else {\n &nbsp; &nbsp; &nbsp;// 新条目：向量分为 0\n &nbsp; &nbsp; &nbsp;byId.set(r.id, {\n &nbsp; &nbsp; &nbsp; &nbsp;...r,\n &nbsp; &nbsp; &nbsp; &nbsp;vectorScore: 0,\n &nbsp; &nbsp; &nbsp; &nbsp;textScore: r.textScore,\n &nbsp; &nbsp;  });\n &nbsp;  }\n  }\n​\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;// Step 2: 加权求和\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;const merged = Array.from(byId.values()).map(entry =&gt; ({\n &nbsp; &nbsp;...entry,\n &nbsp; &nbsp;// ★ 核心公式：score = vectorWeight × vectorScore + textWeight × textScore\n &nbsp; &nbsp;score: params.vectorWeight * entry.vectorScore \n &nbsp; &nbsp; &nbsp; &nbsp; + params.textWeight * entry.textScore,\n  }));\n​\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;// Step 3: 应用时间衰减\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;const decayed = await applyTemporalDecayToHybridResults({\n &nbsp; &nbsp;results: merged,\n &nbsp; &nbsp;config: params.temporalDecay,\n &nbsp; &nbsp;nowMs: params.nowMs,\n  });\n​\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;// Step 4: 按分数降序排序\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;const sorted = decayed.toSorted((a, b) =&gt; b.score - a.score);\n​\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;// Step 5: MMR 重排（如果启用）\n &nbsp;// ═══════════════════════════════════════════════════════\n &nbsp;const mmrConfig = { enabled: false, lambda: 0.7, ...params.mmr };\n &nbsp;if (mmrConfig.enabled) {\n &nbsp; &nbsp;return applyMMRToHybridResults(sorted, mmrConfig);\n  }\n​\n &nbsp;return sorted;\n}\n</code></pre>\n<h2>MMR 去重算法：多样性的数学保证</h2>\n<h3>问题场景</h3>\n<p>假设用户搜索“Agent 架构设计”，返回的前 5 条结果可能都在讲同一个话题的不同段落——内容高度重复，信息增益有限。MMR（Maximal Marginal Relevance）算法通过<strong>惩罚与已选文档相似的候选</strong>来保证多样性。</p>\n<h3>核心公式</h3>\n<p>$$\\text{MMR}(d) = \\lambda \\times \\text{Rel}(d) - (1-\\lambda) \\times \\max_{s \\in S} \\text{Sim}(d, s)$$</p>\n<ul>\n<li>\n<p><code>Rel(d)</code>：文档 d 与查询的相关性（归一化后的 score）</p>\n</li>\n<li>\n<p><code>Sim(d, s)</code>：文档 d 与已选集合 S 中文档的相似度</p>\n</li>\n<li>\n<p><code>λ</code>：权衡参数，默认 0.7（偏向相关性）</p>\n</li>\n</ul>\n<h3>相似度选择：为什么是 Jaccard ？</h3>\n<p>计算文档间相似度有多种选择：</p>\n<p><img src=\"./images/958216_4.png\" alt=\"图片\"></p>\n<p>OpenClaw 选择 Jaccard 相似度，在效率与效果间取得平衡：</p>\n<p>$$\\text{Jaccard}(A, B) = \\frac{|A \\cap B|}{|A \\cup B|}$$</p>\n<h3>完整代码实现</h3>\n<pre><code class=\"language-plain\">// src/memory/mmr.ts\n​\n// ★ 分词函数：将文本转为 token 集合\nfunction tokenize(text: string): Set&lt;string&gt; {\n &nbsp;const tokens = text.toLowerCase().match(/[\\p{L}\\p{N}]+/gu) ?? [];\n &nbsp;return new Set(tokens);\n}\n​\n// ★ Jaccard 相似度计算\nfunction jaccardSimilarity(a: Set&lt;string&gt;, b: Set&lt;string&gt;): number {\n &nbsp;if (a.size === 0 &amp;&amp; b.size === 0) return 1;\n &nbsp;const intersection = [...a].filter(t =&gt; b.has(t)).length;\n &nbsp;const union = new Set([...a, ...b]).size;\n &nbsp;return intersection / union;\n}\n​\n// ★ 计算候选与已选集合的最大相似度\nfunction maxSimilarityToSelected&lt;T extends MMRItem&gt;(\n &nbsp;candidate: T,\n &nbsp;selected: T[],\n &nbsp;tokenCache: Map&lt;string, Set&lt;string&gt;&gt;\n): number {\n &nbsp;if (selected.length === 0) return 0;\n &nbsp;\n &nbsp;const candTokens = tokenCache.get(candidate.id)!;\n &nbsp;let maxSim = 0;\n &nbsp;\n &nbsp;for (const s of selected) {\n &nbsp; &nbsp;const selTokens = tokenCache.get(s.id)!;\n &nbsp; &nbsp;const sim = jaccardSimilarity(candTokens, selTokens);\n &nbsp; &nbsp;if (sim &gt; maxSim) maxSim = sim;\n  }\n &nbsp;\n &nbsp;return maxSim;\n}\n​\n// ★ MMR 分数计算\nfunction computeMMRScore(\n &nbsp;relevance: number,\n &nbsp;maxSimilarity: number,\n &nbsp;lambda: number\n): number {\n &nbsp;return lambda * relevance - (1 - lambda) * maxSimilarity;\n}\n​\n// ═══════════════════════════════════════════════════════\n// MMR 主函数：贪心迭代选择\n// ═══════════════════════════════════════════════════════\nexport function mmrRerank&lt;T extends MMRItem&gt;(\n &nbsp;items: T[],\n &nbsp;config: Partial&lt;MMRConfig&gt; = {}\n): T[] {\n &nbsp;const { enabled = false, lambda = 0.7 } = config;\n &nbsp;\n &nbsp;// ★ 短路返回：未启用或数量不足\n &nbsp;if (!enabled || items.length &lt;= 1) return [...items];\n​\n &nbsp;// ★ 预计算所有文档的 token 集合（缓存优化）\n &nbsp;const tokenCache = new Map&lt;string, Set&lt;string&gt;&gt;();\n &nbsp;for (const item of items) {\n &nbsp; &nbsp;tokenCache.set(item.id, tokenize(item.content));\n  }\n​\n &nbsp;// ★ 分数归一化到 [0, 1]\n &nbsp;const maxScore = Math.max(...items.map(i =&gt; i.score));\n &nbsp;const minScore = Math.min(...items.map(i =&gt; i.score));\n &nbsp;const scoreRange = maxScore - minScore;\n &nbsp;\n &nbsp;const normalizeScore = (score: number): number =&gt; {\n &nbsp; &nbsp;if (scoreRange === 0) return 1;\n &nbsp; &nbsp;return (score - minScore) / scoreRange;\n  };\n​\n &nbsp;// ★ 贪心迭代：每轮选择 MMR 分数最高的候选\n &nbsp;const selected: T[] = [];\n &nbsp;const remaining = new Set(items);\n​\n &nbsp;while (remaining.size &gt; 0) {\n &nbsp; &nbsp;let bestItem: T | null = null;\n &nbsp; &nbsp;let bestMMRScore = -Infinity;\n​\n &nbsp; &nbsp;for (const candidate of remaining) {\n &nbsp; &nbsp; &nbsp;const normalizedRelevance = normalizeScore(candidate.score);\n &nbsp; &nbsp; &nbsp;const maxSim = maxSimilarityToSelected(candidate, selected, tokenCache);\n &nbsp; &nbsp; &nbsp;const mmrScore = computeMMRScore(normalizedRelevance, maxSim, lambda);\n​\n &nbsp; &nbsp; &nbsp;// ★ 平局时用原始分数作为 tiebreaker\n &nbsp; &nbsp; &nbsp;if (mmrScore &gt; bestMMRScore || \n &nbsp; &nbsp; &nbsp; &nbsp;  (mmrScore === bestMMRScore &amp;&amp; candidate.score &gt; (bestItem?.score ?? -Infinity))) {\n &nbsp; &nbsp; &nbsp; &nbsp;bestMMRScore = mmrScore;\n &nbsp; &nbsp; &nbsp; &nbsp;bestItem = candidate;\n &nbsp; &nbsp;  }\n &nbsp;  }\n​\n &nbsp; &nbsp;if (bestItem) {\n &nbsp; &nbsp; &nbsp;selected.push(bestItem);\n &nbsp; &nbsp; &nbsp;remaining.delete(bestItem);\n &nbsp;  } else {\n &nbsp; &nbsp; &nbsp;break;\n &nbsp;  }\n  }\n​\n &nbsp;return selected;\n}\n</code></pre>\n<h3>手动推演示例</h3>\n<p>假设有 3 个候选文档，<code>λ = 0.7</code>，下图展示了完整的三轮迭代过程：</p>\n<p><img src=\"./images/958216_5.jpg\" alt=\"图片\"></p>\n<p><img src=\"./images/958216_6.png\" alt=\"图片\"></p>\n<p><strong>Round 1</strong>（已选集合 S = ∅）：</p>\n<ul>\n<li>\n<p>A：MMR = 0.7×1.0 - 0.3×0 = <strong>0.70</strong> ✓ 最高</p>\n</li>\n<li>\n<p>B：MMR = 0.7×0.8 - 0.3×0 = 0.56</p>\n</li>\n<li>\n<p>C：MMR = 0.7×0.6 - 0.3×0 = 0.42</p>\n</li>\n</ul>\n<p>选择 A，S = {A}</p>\n<p><strong>Round 2</strong>（S = {A}）：</p>\n<ul>\n<li>\n<p>B：Jaccard(B, A) = 0.6（内容相似），MMR = 0.7×0.8 - 0.3×0.6 = 0.56 - 0.18 = <strong>0.38</strong></p>\n</li>\n<li>\n<p>C：Jaccard(C, A) = 0.1（内容不同），MMR = 0.7×0.6 - 0.3×0.1 = 0.42 - 0.03 = <strong>0.39</strong> ✓ 最高</p>\n</li>\n</ul>\n<p>选择 C！尽管 B 的相关性更高，但因为与 A 太相似而被惩罚。</p>\n<p><strong>最终排序</strong>：A → C → B（多样性得到保证）</p>\n<h2>时间衰减机制：让记忆有“保鲜期”</h2>\n<h3>设计动机</h3>\n<p>并非所有记忆都应该拥有相同的权重。昨天的对话比去年的对话更可能与当前任务相关。时间衰减通过<strong>指数衰减模型</strong>实现“近期优先”。</p>\n<h3>数学模型</h3>\n<p>$$\\text{decay} = e^{-\\lambda \\times \\text{age\\_days}}$$</p>\n<p>其中 <code>λ = ln(2) / halfLifeDays</code>，<code>halfLifeDays</code> 默认为 30 天。</p>\n<p>这意味着：</p>\n<ul>\n<li>\n<p>今天的记忆：decay = 1.0（无衰减）</p>\n</li>\n<li>\n<p>30 天前的记忆：decay = 0.5（半衰）</p>\n</li>\n<li>\n<p>60 天前的记忆：decay = 0.25</p>\n</li>\n<li>\n<p>90 天前的记忆：decay = 0.125</p>\n</li>\n</ul>\n<h3>核心代码实现</h3>\n<pre><code class=\"language-plain\">// src/memory/temporal.ts\n​\nexport function calculateTemporalDecayMultiplier(params: {\n &nbsp;ageInDays: number;\n &nbsp;halfLifeDays: number; &nbsp;// 默认 30\n}): number {\n &nbsp;// ★ 半衰期转衰减常数\n &nbsp;const lambda = Math.LN2 / params.halfLifeDays;\n &nbsp;\n &nbsp;// ★ 防御性处理：负数年龄视为 0\n &nbsp;const clampedAge = Math.max(0, params.ageInDays);\n &nbsp;\n &nbsp;// ★ 指数衰减公式\n &nbsp;return Math.exp(-lambda * clampedAge);\n}\n</code></pre>\n<h3>时间戳获取：三层优先级</h3>\n<p>获取文档的“年龄”需要确定其时间戳，OpenClaw 采用三层优先级策略：</p>\n<pre><code class=\"language-plain\">// src/memory/temporal.ts\n​\nexport function extractTimestamp(params: {\n &nbsp;filePath: string;\n &nbsp;fileStats?: { mtimeMs: number };\n}): { timestamp: number; isEvergreen: boolean } {\n &nbsp;\n &nbsp;// ★ 优先级 1：从文件路径解析日期\n &nbsp;// 匹配 memory/2026-03-19.md 格式\n &nbsp;const dateMatch = params.filePath.match(/(\\d{4})-(\\d{2})-(\\d{2})/);\n &nbsp;if (dateMatch) {\n &nbsp; &nbsp;const [_, year, month, day] = dateMatch;\n &nbsp; &nbsp;const timestamp = new Date(`${year}-${month}-${day}`).getTime();\n &nbsp; &nbsp;return { timestamp, isEvergreen: false };\n  }\n​\n &nbsp;// ★ 优先级 2：检测常青记忆（MEMORY.md）\n &nbsp;const basename = path.basename(params.filePath).toLowerCase();\n &nbsp;if (basename === \"memory.md\") {\n &nbsp; &nbsp;// 常青记忆：返回当前时间，永不衰减\n &nbsp; &nbsp;return { timestamp: Date.now(), isEvergreen: true };\n  }\n​\n &nbsp;// ★ 优先级 3：使用文件修改时间\n &nbsp;if (params.fileStats?.mtimeMs) {\n &nbsp; &nbsp;return { timestamp: params.fileStats.mtimeMs, isEvergreen: false };\n  }\n​\n &nbsp;// 兜底：当前时间\n &nbsp;return { timestamp: Date.now(), isEvergreen: false };\n}\n</code></pre>\n<h3>常青记忆：永不过时的知识</h3>\n<p><code>MEMORY.md</code> 文件存储用户手动维护的长期知识（如个人偏好、项目背景），这类信息不应随时间衰减。系统通过 <code>isEvergreen</code> 标志识别并跳过衰减计算：</p>\n<pre><code class=\"language-plain\">// 应用衰减时的判断逻辑\nif (timestampInfo.isEvergreen) {\n  // ★ 常青记忆：decay = 1.0，不衰减\n  result.score = result.score * 1.0;\n} else {\n  const ageInDays = (nowMs - timestampInfo.timestamp) / (1000 * 60 * 60 * 24);\n  const decay = calculateTemporalDecayMultiplier({ ageInDays, halfLifeDays });\n  result.score = result.score * decay;\n}\n</code></pre>\n<h2>总结</h2>\n<p>经过这节课的学习，你应该掌握以下要点：</p>\n<ol>\n<li>\n<p><strong>混合搜索</strong>：70% 向量 + 30% 关键词的双引擎设计，<code>buildFtsQuery</code> 实现 Unicode 感知分词</p>\n</li>\n<li>\n<p><strong>BM25 转换</strong>：<code>bm25RankToScore</code> 将负数 rank 映射到 (0, 1)，公式为 <code>relevance / (1 + relevance)</code></p>\n</li>\n<li>\n<p><strong>五步合并</strong>：按 ID 去重 → 加权求和 → 时间衰减 → 排序 → MMR 重排</p>\n</li>\n<li>\n<p><strong>MMR 算法</strong>：基于 Jaccard 相似度的贪心迭代，<code>λ=0.7</code> 平衡相关性与多样性</p>\n</li>\n<li>\n<p><strong>时间衰减</strong>：指数衰减模型，30 天半衰期，常青记忆（MEMORY.md）永不衰减</p>\n</li>\n<li>\n<p><strong>工程思维</strong>：选择 Jaccard 而非余弦相似度，是“够用即可”的务实决策</p>\n</li>\n</ol>\n<p>到这里，我们已经打开了 <code>search()</code> 方法里的所有算法黑盒。但还有一个更底层的问题没有回答：</p>\n<ul>\n<li>\n<p><strong>向量从哪来？</strong> <code>embedQuery()</code> 背后是什么？</p>\n</li>\n<li>\n<p><strong>数据怎么存？</strong> 六张表的 Schema 如何设计？</p>\n</li>\n<li>\n<p><strong>查询怎么优化？</strong> 分块策略和索引重建有哪些工程细节？</p>\n</li>\n</ul>\n<p>下节课，我们将深入<strong>数据基础设施层</strong>，看看 Embedding 多提供者体系、SQLite 存储设计以及 <code>chunkMarkdown</code> 分块算法的实现。</p>\n<h2>思考题</h2>\n<ol>\n<li>\n<p><strong>参数调优</strong>：如果你的应用场景是“法律文档检索”，需要极高的精确度而非多样性，你会如何调整 <code>vectorWeight</code>、<code>textWeight</code> 和 <code>MMR.lambda</code> 三个参数？</p>\n</li>\n<li>\n<p><strong>算法改进</strong>：当前的 MMR 使用 Jaccard 相似度，如果你愿意牺牲一些性能换取更好的语义去重效果，可以如何改进？提示：考虑复用已有的 Embedding 向量。</p>\n</li>\n<li>\n<p><strong>边界情况</strong>：假设某个 chunk 既没有文件路径日期，也没有文件修改时间（<code>fileStats</code> 为空），当前代码会返回 <code>Date.now()</code>。这个设计合理吗？有没有更好的默认策略？</p>\n</li>\n</ol>\n<p>欢迎你在留言区与我交流！如果今天的课程让你有所收获，也欢迎转发给有需要的朋友，邀请他来一起学习，我们下节课再见！</p>\n","comments":[{"had_liked":false,"id":407058,"user_name":"猛禽不是鸟","can_delete":false,"product_type":"c1","uid":1068707,"ip_address":"北京","ucode":"19C1E2E558C847","user_header":"https://static001.geekbang.org/account/avatar/00/10/4e/a3/7c685adf.jpg","comment_is_top":false,"comment_ctime":1776819782,"is_pvip":false,"replies":null,"discussion_count":0,"race_medal":0,"score":2,"product_id":101123001,"comment_content":"我认为混合检索的比例应该是向量的占比低一些，因为法律行业要求的精准，向量的核心思想是语义相似度，可能存在语义主题内容高度相似，但是语义的立场不同。例如：我被人打了怎么办，我把人打了怎么办。这两个句子只查一个字，语义立场不同，大多数模型的相关性会高于0.5。\n再说mmr去重，我个人感觉在法律行业可以将权衡参数调整到0.1或者0.2左右。法律行业特殊性在于要求查询到的内容高度相似，例如我就是查询北京地区关于打人的类案，法院判决的时候会参考有效法规下的类案。所以是要高度相似的，存在mmr是为了又一点扩展性，例如都是北京打人，打的伤势如何，对方有无提出谅解条件等，都是需要扩展出来的。","like_count":0},{"had_liked":false,"id":406790,"user_name":"Geek_c67146","can_delete":false,"product_type":"c1","uid":3678249,"ip_address":"广东","ucode":"3484363F8AA418","user_header":"","comment_is_top":false,"comment_ctime":1776001002,"is_pvip":false,"replies":null,"discussion_count":0,"race_medal":0,"score":2,"product_id":101123001,"comment_content":"很难","like_count":0}]}