{"id":966978,"title":"22｜Provider、Skill 与 Channel 实战：如何用三种插件类型覆盖 80% 开发场景？","content":"<p>你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。</p>\n<p>前面我们学了插件的骨架（定义 + API）和神经系统（Hook），理解了框架的运行机制。这一节我们聚焦“肌肉”——三种让 Agent 真正“有用”的核心插件类型。为了方便理解，我们把OpenClaw比作一个正在成长的人，那么它的核心循环就是维持生命的心跳。然而，一个只有心跳的躯壳是无法感知世界并采取行动的。它需要“脑子”来思考，需要“技能”来处理具体任务，还需要“耳朵和嘴巴”来与外界交流。在 OpenClaw 的设计哲学中，这三者分别对应了 Provider、Skill 和 Channel 三种核心插件：</p>\n<ul>\n<li>\n<p>Provider：能调用 LLM 模型，让 Agent 有“脑子”</p>\n</li>\n<li>\n<p>Skill：知道怎么查天气、管 GitHub，让 Agent 有“技能”</p>\n</li>\n<li>\n<p>Channel：能接收和发送消息，让 Agent 有“耳朵和嘴巴”</p>\n</li>\n</ul>\n<p>为什么把这三种放在一起？它们代表 Agent 能力的<strong>三个基本维度</strong>——思考（Provider）、行为（Skill）、交互（Channel）。理解了这三种，你就覆盖了 OpenClaw 插件开发最常见的 <strong>80%</strong> 场景。</p>\n<p><img src=\"./images/966978_1.png\" alt=\"图片\"></p>\n<p>下面我们首先讲解Provider 体系，Provider 是最重要的插件类型，没有 Provider，Agent 连模型都调不了。</p><!-- [[[read_end]]] -->\n<h2>Provider 体系：为 Agent 装上最强大脑</h2>\n<p>Provider插件本质是工厂模式与策略模式的结合。OpenClaw 定义了统一的 <code>ProviderPlugin</code> 接口，确保了底层基础设施的可替换性。无论你接入的是 OpenAI、Anthropic 还是本地部署的 Llama，对于框架来说，它们都是符合标准的“脑子”。</p>\n<p>OpenClaw 内置的 33 种 Provider 全部通过 <code>registerProvider</code> 注册。Provider 是<strong>基础设施级</strong>的插件。</p>\n<p>下面是ProviderPlugin 完整接口示例：</p>\n<pre><code class=\"language-plain\">export type ProviderPlugin = {\n  id: string;\n  label: string;\n  aliases?: string[];\n\n  // 认证——支持 5 种方式\n  auth: ProviderAuthMethod[];\n  // auth.kind: \"oauth\" | \"api_key\" | \"token\" | \"device_code\" | \"custom\"\n\n  // 模型目录——静态 + 动态 + 增强\n  catalog?: ProviderPluginCatalog;\n  resolveDynamicModel?: (ctx) =&gt; ProviderRuntimeModel | null;\n  augmentModelCatalog?: (ctx) =&gt; ModelCatalogEntry[] | null;\n\n  // 流式处理\n  wrapStreamFn?: (ctx) =&gt; StreamFn | null;\n\n  // 能力声明\n  capabilities?: Partial&lt;ProviderCapabilities&gt;;\n\n  // 思考模式\n  isBinaryThinking?: (ctx) =&gt; boolean | undefined;\n  supportsXHighThinking?: (ctx) =&gt; boolean | undefined;\n  resolveDefaultThinkingLevel?: (ctx) =&gt; ThinkLevel | null;\n\n  // 运行时认证\n  prepareRuntimeAuth?: (ctx) =&gt; Promise&lt;ProviderPreparedRuntimeAuth | null&gt;;\n  resolveUsageAuth?: (ctx) =&gt; Promise&lt;ProviderResolvedUsageAuth | null&gt;;\n  fetchUsageSnapshot?: (ctx) =&gt; Promise&lt;ProviderUsageSnapshot | null&gt;;\n\n  // 设置向导\n  wizard?: ProviderPluginWizard;\n};\n\n</code></pre>\n<p>我们可以看到，Provider接口设计得如此丰富，并不是过度设计，而是因为不同 Provider 差异极大。</p>\n<ul>\n<li>\n<p>OpenAI 用 API Key，Google 用 OAuth，Ollama 免认证</p>\n</li>\n<li>\n<p>模型目录有静态的（已知列表）和动态的（用户自定义 ID）</p>\n</li>\n<li>\n<p>流式输出格式各家不同（SSE、WebSocket、gRPC）</p>\n</li>\n<li>\n<p><strong>丰富的接口 = 最大的灵活性</strong>，每个 Provider 只需实现自己需要的部分</p>\n</li>\n</ul>\n<h3>五种认证方式与细分类型</h3>\n<p>为了应对不同厂商的差异，OpenClaw 提供了极其灵活的认证体系。开发者可以根据实际情况选择 <code>oauth</code>、<code>api_key</code>、<code>token</code>、<code>device_code</code> 或 <code>custom</code> 认证方式。</p>\n<p>在实际应用中，Provider 被细分为五种类型，涵盖了 AI 能力的全景：</p>\n<ol>\n<li>\n<p>ProviderPlugin (LLM)：核心推理引擎</p>\n</li>\n<li>\n<p>SpeechProviderPlugin (TTS)：赋予 Agent 说话的能力</p>\n</li>\n<li>\n<p>MediaUnderstandingProvider：让 Agent 看懂图片和视频</p>\n</li>\n<li>\n<p>ImageGenerationProvider：让 Agent 具备创作能力</p>\n</li>\n<li>\n<p>WebSearchProviderPlugin：为 Agent 提供实时联网搜索的“外挂”</p>\n</li>\n</ol>\n<p><img src=\"./images/966978_2.png\" alt=\"图片\"></p>\n<p>了解了如何构建 Agent 的“大脑”后，我们接下来要解决如何让这个大脑学会具体的任务，这就是 Skill 系统的用武之地。</p>\n<h2>Skill 技能系统：赋予 Agent 专业能力</h2>\n<p>如果说 Provider 是通用的智力，那么 Skill 就是具体的专业技能。OpenClaw 的 Skill 系统遵循一个核心理念：<strong>不修改代码逻辑，仅通过注入 System Prompt 引导行为</strong>。</p>\n<p>Skill 的设计理念极简，本质是把专家知识写成 Markdown，让 LLM 按照你的指引使用工具。开发门槛极低，不懂编程的领域专家也能创建 Skill。</p>\n<p>一个标准的 <code>SKILL.md</code> 包含以下核心要素：</p>\n<ul>\n<li>\n<p><strong>元数据</strong>：定义技能的 ID、名称和版本。</p>\n</li>\n<li>\n<p><strong>依赖检查</strong>：通过 <code>bins</code>（二进制文件）、<code>env</code>（环境变量）和 <code>os</code>（操作系统）确保运行环境。</p>\n</li>\n<li>\n<p><strong>行为指引</strong>：告诉 LLM 在什么场景下使用该技能，以及具体的 SOP。</p>\n</li>\n</ul>\n<p>以 weather 这个Skill为例：</p>\n<pre><code class=\"language-plain\">---\nname: weather\ndescription: \"Get current weather and forecasts via wttr.in or Open-Meteo\"\nhomepage: https://wttr.in/:help\nmetadata:\n &nbsp;{\n &nbsp; &nbsp;\"openclaw\":\n &nbsp; &nbsp; &nbsp;{\n &nbsp; &nbsp; &nbsp; &nbsp;\"emoji\": \"☔\",\n &nbsp; &nbsp; &nbsp; &nbsp;\"requires\": { \"bins\": [\"curl\"] }\n &nbsp; &nbsp; &nbsp;}\n &nbsp;}\n---\n​\n# Weather Skill\n​\n当用户询问天气时，使用 curl 命令查询 wttr.in。\n​\n## When to Use\n- User asks about weather conditions\n- User wants to know the forecast\n​\n## Commands\n- `curl \"wttr.in/{city}?format=3\"` — 简短天气\n- `curl \"wttr.in/{city}\"` — 详细天气报告\n</code></pre>\n<p>核心字段主要如下：</p>\n<ul>\n<li>\n<p><code>name</code> / <code>description</code>：基础元数据</p>\n</li>\n<li>\n<p><code>metadata.openclaw</code>：嵌套结构（⚠️不是 frontmatter 顶层的 <code>requires</code>）</p>\n<ul>\n<li>\n<p><code>emoji</code>：显示表情</p>\n</li>\n<li>\n<p><code>requires.bins</code>：依赖的二进制（如 curl、gh）</p>\n</li>\n<li>\n<p><code>requires.env</code>：依赖的环境变量</p>\n</li>\n</ul>\n</li>\n</ul>\n<h3>门控机制与加载优先级</h3>\n<p>为了确保系统的健壮性，OpenClaw 引入了自动门控机制。系统会自动检测环境，如果某个技能依赖的二进制工具不存在，该技能将不会被激活。</p>\n<p>Skill 是否加载取决于以下检查：</p>\n<ol>\n<li>\n<p><strong>bins 检查</strong> → 依赖的二进制是否存在（如 curl、gh）</p>\n</li>\n<li>\n<p><strong>env 检查</strong> → 依赖的环境变量是否已设置</p>\n</li>\n<li>\n<p><strong>config 检查</strong> → 依赖的配置项是否存在</p>\n</li>\n<li>\n<p><strong>os 检查</strong> → 当前操作系统是否匹配</p>\n</li>\n<li>\n<p><strong>always 标记</strong> → 如果为 true，跳过所有门控</p>\n</li>\n<li>\n<p><strong>配置覆写</strong> → <code>config.skills.entries.&lt;name&gt;.enabled</code> 可强制启用/禁用</p>\n</li>\n</ol>\n<p>同时，Skill 的加载遵循严格的六级优先级，确保了配置的灵活性。</p>\n<p><img src=\"./images/966978_3.png\" alt=\"图片\"><br>\n<img src=\"./images/966978_4.png\" alt=\"图片\"></p>\n<p>这种设计确保了即使在复杂的项目中，Prompt 也不会因为过度堆砌而导致模型迷失方向。</p>\n<p>既然 Agent 已经有了脑子和技能，它还需要一个通道来接收指令并输出结果。接下来，我们将探讨如何构建 Agent 的“感官”。</p>\n<h2>Channel Plugin：连接现实世界的“耳朵和嘴巴”</h2>\n<p>Channel 插件负责解决 Agent 与外部通信系统的对接问题。它采用了经典的“通道适配器（Channel Adapter）”模式，将 Agent 生成的结构化消息转换为特定平台（如 Slack、Telegram）的协议格式。</p>\n<h3>适配器组合模式</h3>\n<p>Channel 的强大之处在于其模块化的适配器设计。开发者可以像搭积木一样组合不同的能力：</p>\n<ul>\n<li>\n<p><strong>核心适配器</strong>：处理 <code>config</code>（配置）、<code>outbound</code>（出站消息）、<code>gateway</code>（网关接入）和 <code>messaging</code>（基础消息）。</p>\n</li>\n<li>\n<p><strong>高级适配器</strong>：支持 <code>streaming</code>（流式输出）、<code>threading</code>（会话线程）和 <code>groups</code>（群组管理）。</p>\n</li>\n</ul>\n<p><img src=\"./images/966978_5.png\" alt=\"图片\"></p>\n<h3>竞争认领机制：inbound_claim</h3>\n<p>当一条消息从外部进入系统时，OpenClaw 如何决定由哪个插件处理？答案是 <code>inbound_claim</code> Hook。</p>\n<p>这是一个典型的“竞争认领”模式：</p>\n<ol>\n<li>\n<p>消息到达网关。</p>\n</li>\n<li>\n<p>框架触发 <code>inbound_claim</code> Hook。</p>\n</li>\n<li>\n<p>所有注册的 Channel 插件根据消息特征（如 URL 路径、Header 标识）进行判断。</p>\n</li>\n<li>\n<p>第一个返回 <code>true</code> 的插件将获得该消息的处理权。</p>\n</li>\n</ol>\n<p>目前 OpenClaw 已内置了 21 个通道，覆盖了从企业级的飞书、Slack 到个人社交的 Telegram、WhatsApp 等主流平台。</p>\n<h2>总结</h2>\n<p>在实际开发中，我们往往需要组合这三种能力。例如，构建一个“专业翻译Agent”：</p>\n<ol>\n<li>\n<p><strong>Provider</strong>：接入 DeepL 或 GPT-4 作为翻译引擎。</p>\n</li>\n<li>\n<p><strong>Skill</strong>：编写 <code>SKILL.md</code>，定义翻译的风格（如信达雅）和术语表。</p>\n</li>\n<li>\n<p><strong>Channel</strong>：对接 Telegram，让用户可以随时随地发送文字进行翻译。</p>\n</li>\n</ol>\n<p>通过这三种插件类型的配合，开发者无需触碰 OpenClaw 的核心代码，即可实现 80% 以上的业务需求。这种高度解耦的架构，正是 OpenClaw 能够快速构建繁荣生态的基石。</p>\n<h2>思考题</h2>\n<p>需要为一个私有部署的医疗大模型开发接入插件，该模型要求使用动态生成的 <code>device_code</code> 进行认证，且输出结果需要经过一个本地的脱敏工具处理。</p>\n<p>你应该选择哪种 Provider 认证方式？如何利用 Skill 的依赖检查（bins）确保脱敏工具在运行前已就绪？如果该 Agent 需要在医院内部的钉钉群中使用，你需要配置 Channel 的哪些适配器？</p>\n<p>欢迎你在留言区与我交流！如果今天的课程让你有所收获，也欢迎转发给有需要的朋友，邀请他来一起学习，我们下节课再见！</p>\n","comments":[]}